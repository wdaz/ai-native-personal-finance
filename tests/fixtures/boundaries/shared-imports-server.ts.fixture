// Linted as src/shared/imports-server.ts.
// ADR-0002: shared imports nothing from the rest. It is the one layer the client bundle,
// the route handlers and the tools all load, so a server import here would drag the
// database into the browser.
import "@/src/server/db";
