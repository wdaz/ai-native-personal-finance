// Linted as app/(app)/overview/imports-prisma.ts.
// ADR-0002: app never imports Prisma directly — it reaches the database through server.
import { PrismaClient } from "@prisma/client";

export const client = new PrismaClient();
