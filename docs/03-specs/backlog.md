# Backlog — Release 1 (vertical slice: Auth + Overview)

Status: **Approved** (v1.30 — 2026-09-25: T-13a's plan-gate answers and its execution — `proxy.ts` without `runtime`, two API tests pinning the headers and the matcher, the live wording says "proxy"; hand-offs to T-13b, T-13d and T-14; v1.29 — 2026-09-25: the T-13c plan is merged too (PR #41); v1.28 — 2026-09-25: T-13c is merged (PR #39); `tech-debt.md` v1.13 closes TD-7–TD-11; v1.27 — 2026-09-25: T-13c's plan-gate answers (Q1–Q6) and its execution — Stylelint as a dev dependency, TD-8's comment without a failing-first test, TD-10's first seed and scope, `db:reset` refused before `migrate deploy` (owner's choice "B"), the Public Sans files; hand-offs to T-13d, T-14 and T-16; v1.26 — 2026-09-24: CodeQL runs as an advanced workflow with the `security-and-quality` suite instead of default setup — owner decision, PR #37; T-16's item says so; v1.25 — 2026-09-24: T-13c also fixes TD-11, the build's download of Public Sans from Google Fonts — owner decision "a"; v1.24 — 2026-09-24: four tasks before T-14 — T-13a (TD-2), T-13b (TD-3), T-13c (TD-7–TD-10) and T-13d (security review) — owner decision; T-14's T-02 guard hand-off moves to T-13c; v1.23 — 2026-09-24: the T-15/T-16 rows are one table again; `verify`'s checkout without persisted credentials; the ADR-0003 acceptance's Release 2 condition handed to T-15; v1.22 — 2026-09-24: T-13 executed, hand-offs to T-14, T-15 and T-16; v1.21 — 2026-09-24: T-12's hand-offs to T-13, T-14, T-15 and T-16; v1.20 — 2026-09-23: T-08's hand-offs to T-11, T-14, T-15 and T-16, owner decision; v1.19 — 2026-09-23: T-07 plan gate — the logout fallback, TD-1 to T-07, a heading-only Overview page, the T-08/T-10/T-11/T-16 hand-offs; v1.18 — 2026-09-23: T-06's review — TD-4 and TD-5 open, the page-title rule handed to T-07/T-10; v1.17 — 2026-09-23: tech debt moves to its own file, `tech-debt.md`, linked from the Notes; a T-16 note on home-directory paths in the process records; v1.16 — 2026-09-23: T-06 plan gate — US-03 AC1–AC2 E2E moves to T-07, a T-10 hand-off for US-01 AC1's Overview assertion; v1.15 — 2026-09-23: T-05's whole-branch review, C2 fix — a hand-off note in the T-06 row for reading the CSP nonce; v1.14 — 2026-09-23: T-05's whole-branch review — a hand-off note in the T-07 row for the no-store test T-05 could not isolate from Next's 404 default; v1.13 — 2026-09-23: T-05 plan gate — `latestResetAt` lands in T-05, a hand-off note in the T-08 row; v1.12 — 2026-09-23: T-05's hand-off gains a timing-side-channel note from the login-401 change's review; v1.11 — T-05's hand-off — login never sends 400, a malformed body folds into 401 `invalid_credentials` (SPEC-auth v1.0.3, security); v1.10 — T-05's hand-off corrected — the 400 validation body has no `message` (SPEC-auth v1.0.2); v1.9 — 2026-09-23: T-04 hand-offs written into T-05/T-06/T-07/T-08/T-09/T-10/T-11/T-12/T-13/T-15, owner decisions at the T-04 plan gate; v1.8 — 2026-09-22: T-03 takes the §4.2 formatters and the ADR-0005 rule fix, T-04 narrowed, T-03 hand-offs written into T-05/T-08/T-09/T-10/T-13, owner decisions at the T-03 plan gate; v1.7 — 2026-09-22: T-02 hand-offs written into T-05/T-06/T-08/T-09/T-13/T-14 rows; v1.6 — 2026-09-22: T-02/T-05/T-12 test-support and CI scope, T-13 overrides removal, owner decisions at the T-02 plan gate; v1.5 — 2026-09-22: T-13 permissions + message-scan evaluation, T-16 ignore-allow + guard-file ownership (T-02a hand-off); v1.4 — 2026-09-22: T-13 `master` trigger removed in T-02a, T-16 scan wording — owner decisions at the T-02a plan gate; v1.3 — 2026-09-20: T-16 licence steps; v1.2 — 2026-09-20: fixture repointing in T-02/T-03/T-04, CI concurrency in T-13; v1.1 T-02a/T-16; v1.0 approved 2026-09-20) · Author(s): Agent · Date: 2026-09-20
Changelog: v1.30 (2026-09-25, T-13a plan gate and execution, branch `task/T-13a-proxy`) — the owner answered
the T-13a plan's Q1–Q6 ("1. razı — 2. bəli — 3. Bunu müzakirə edək. Linterin buna nə aidiyyatı. —
4. Bəli dəyişsin — 5. yes — 6. Bəli. Amma hələ icraya razılıq verməmişəm."; after the discussion of
Q3: "Cavab a. pr yenilə merge edim."). Q1 agreed: TD-2's Fix line said to keep `runtime: "nodejs"`,
which Next 16.3.5 refuses in a proxy, so the option goes and TD-2 records the correction. Q2 yes:
two API tests pin the security headers and the matcher before the rename. Q3 (a): no guard against
a lone `middleware.ts` returning — the lint-rule alternative that came up is recorded in the plan's
Q3. Q4 yes: every live reference says "proxy", prose included. Q5 yes: the wording-only texts (four
specs and one diagram box); ADR-0006 amendment (6) stays *proposed* until the owner accepts it. Q6
yes: one PR, four commits, executed inline, one Opus 5.5 review of the whole branch. The plan was
merged as PR #43 and the go-ahead followed ("main brache keç plan artıq ordadır. İcraya başla").
The Notes gain an "Open at v1.30" snapshot (TD-2 in review, TD-3 open) beside the older ones,
which stay as written, and T-13b, T-13d and T-14 gain a hand-off each; the T-13a row itself is
unchanged. v1.29 (2026-09-25, branch `docs/T-13c-plan-merged`) — the owner merged the T-13c plan
(branch `docs/T-13c-plan`, PR #41, status Done, v0.3) and the closure PR #40. The T-13c row and
the two prompts records (`prompts/2026-09-24-T-13c.md` and its folder's README) no longer say that
the plan awaits a merge or a PR; nothing else changes. v1.28 (2026-09-25, branch
`docs/T-13c-closed`) — the owner merged PR #39 (T-13c, merge
`8ffe0b1`, CI green on its last head `f5e7a1c`). The T-13c row says so, and `tech-debt.md` v1.13
marks TD-7–TD-11 **Closed**. Nothing else changes: the hand-offs v1.27 wrote into T-13d, T-14 and
T-16 stand, and the Notes' "Open at v1.18" line is a snapshot and stays. v1.27 (2026-09-25, T-13c
plan gate and execution, branch `task/T-13c-tech-debt`) — the
owner answered the T-13c plan's Q1–Q6 ("Q1 - a, Q2 - tövsiyə olan, q3 - tövsiyyə olan, q4 - linter,
q5 - tövsiyyə olan, q6 - tövsiyyə olan") and asked whether T-13a/b belong in that plan (Q7). (a) for
TD-8: the comment says what the test does, and the failing-first line of the T-13c row is waived
for it, since a comment cannot be red. (a) for TD-10's first seed and scope: Neon is seeded by
`POST /api/admin/reset` (SPEC-reset-and-test-support v1.6), and any `VERCEL` or `VERCEL_ENV` refuses
`APP_ENV=test`. **A linter for TD-9, not the unit test the plan recommended: Stylelint 17.15.0, a
new dev dependency** (75 packages, `npm audit` 0; `npm run lint` runs it). (A) for TD-11: the static
latin 400 and 700 files of `@fontsource/public-sans` 5.3.0, checked by an ESLint restriction and a
provenance test. T-13a and T-13b are separate tasks with their own plan gates, not part of that
plan; T-13c is independent of both, as the Notes say. Four row edits: T-13c names its plan and the
answers; T-13d's dependency review (item 4) gains `stylelint`; T-14 gains the first-seed and
`VERCEL` hand-off; T-16's third-party notices gain Public Sans. From the whole-branch review of
T-13c, two more items, both to check and neither measured here: T-13d's item (3) gains the stock
Prisma commands that reset a database (`prisma migrate reset`, `prisma db push --force-reset` and
`migrate dev`'s reset prompt), which the TD-10 guard does not cover, and T-14 gains a check of
whether `vercel env pull` writes `.env.local`, and of what the local guards then do. T-14's
sentence on `db:reset` was also reworded, and again after the owner's choice "B" (2026-09-25,
made after PR #39 was opened, on the review's finding that `prisma migrate deploy` would already
have applied every pending migration by the time the seed refused): `prisma.config.ts` refuses a
non-local `DATABASE_URL` for `npm run db:reset` before `prisma migrate deploy` applies any
migration, so `db:reset` refuses outright; a direct `npx prisma migrate deploy`, which T-14 runs
against Neon, is not guarded. T-13d's item 3 was edited for the same follow-up: it now says that
nothing refuses the stock Prisma reset commands outside `npm run db:reset`. v1.26 (2026-09-24,
branch `docs/accept-adr-0006-5`) — the owner replaced CodeQL's default setup with an advanced
workflow, `.github/workflows/codeql.yml` (PR #37), because they want stronger
checks: "Səbəb daha advance yoxlamanın olmasını istəyirəm." The suite is
`security-and-quality`, which contains `security-extended` and is available only in advanced setup,
chosen after a measurement on PR #37 ("Əvvəl ölç, nəticəni göstər" → "Bəli, B-də qal"): 201
JavaScript/TypeScript rules instead of 87, 27 Actions rules instead of 17, 245 files, 0 results.
T-16's "CodeQL default setup (JS/TS)" is struck through with a pointer. v1.25 (2026-09-24, branch `docs/accept-adr-0006-5`) — two CI legs failed in the same
minute at `next build`, because the build could not download Public Sans from Google Fonts
(`tech-debt.md` TD-11). The owner chose "a": T-13c also serves the font from committed files, and
`design-tokens.md` §Typography follows. v1.24 (2026-09-24, owner decisions before T-14, branch `docs/pre-T-14-plan`) — the
owner asked for the tech debt to be fixed before the deploy, each fix a task that the process log
records like any other ("T14 keçməzdən öncə Tech deptləri düzəltmək. Onlarda Tasklar kimi
prosess-log-da öz əksini tapmalıdır."), and for a security check. Four rows go between T-13 and
T-14, lettered as T-02a was: **T-13a** fixes TD-2, **T-13b** TD-3, **T-13c** four items that had no
entry and now do (`tech-debt.md` v1.10: TD-7 the WebMCP indicator, TD-8 a test comment, TD-9 the
card-grid rule, TD-10 the guard against a non-local database, which leaves T-14's row), and
**T-13d** is the security review, last, so it reads the code that T-14 deploys. The owner chose
each option in a structured question: the review after the debt, all four unregistered items, an
exploitable finding kept in a private security advisory until fixed, and "no secret was real
before the repository went public" (NFR-S5). T-14 depends on T-13d. The release strategy is
changing (owner, next session), so T-15 and T-16 are not touched here. v1.23 (2026-09-24, owner decisions after T-13, branch `chore/T-13-followups`) — a blank
line between the T-15 and T-16 rows ended the table, so T-16 rendered as a paragraph; the rows are
one table again (no text changed). The `verify` job's checkout now sets `persist-credentials:
false` like the other four (owner decision; T-13 Task 1 review, deferred minor), and T-16's item
(4) says so. The owner accepted the ADR-0003 traceability clarification on
the condition that the next release reflects it: T-15's row now says the story list is regenerated
for Release 2 and the script reads the release being built. v1.22 (2026-09-24, T-13 execution, branch `task/T-13-ci-hardening`) — the T-13 row
is delivered except what it says to keep: the workflow token is read-only and the `concurrency`
group is split; the coverage gate (90 % of statements on `src/domain`), the traceability script
scoped to `release-1-stories.txt`, the schema-vs-migrations drift check, the axe gate on every
route, the PR template with the DoD checklist, `strict-allow-scripts=true` (with `fsevents`
denied and `engines.npm` `>=11.19`), the commit/tag-message pass of the secret scan, TD-4 (closed)
and Firefox and WebKit in the E2E job (four legs) are in; `vite-tsconfig-paths` is gone and
`coverage.include` is narrowed. Plan finding F1 — Firefox and WebKit failed the WebMCP tests
because they report `originAgentCluster === false` without an `Origin-Agent-Cluster` header —
was fixed first, as its own PR (#28, "PR-A"); T-12 had merged with those two engines red. The
Prisma `overrides` stay (plan finding F4): re-measured on 2026-09-24, npm audit reports 4 high
without them, and the removal moves to T-16 (owner decision, Q7). Hand-offs, as in v1.21: **T-14**
(npm 11.19 on Vercel), **T-15** (what the review of T-12 and T-13 found), **T-16** (the required
checks, the overrides, what the first CI run must show). The dated "Open at v1.18" line in the
Notes is a snapshot and is not rewritten. v1.21 (2026-09-24, T-12 execution) — hand-offs written into the T-13, T-14, T-15 and
T-16 rows, as in v1.7–v1.9, v1.19 and v1.20. T-13: the E2E job is already a `webmcp-mode` matrix.
T-14: `deploy.md` links to the native runbook. T-15: what `getTools()` hides and what a tool never
receives. T-16: the two E2E legs are two check names. v1.20 (2026-09-23, owner decision after T-08: "Bəli. Hamısı bir pr-da", meaning
the hand-offs ride in T-08's PR) — hand-offs written into the T-11, T-14, T-15 and T-16 rows,
as in v1.7–v1.9 and v1.19. T-14: production needs `CRON_SECRET`, or Vercel's scheduled call
arrives without an `Authorization` header and gets a 401. T-11: `Shell` receives `meta`.
T-15: `checkThreshold`'s call sites for Release 2. T-16: the source of `CloseCircleIcon`.
v1.19 (2026-09-23, owner decisions at the T-07 plan gate, Q1–Q4 — all as recommended) — the T-07 row: the `(app)` layout carries the origin-trial tag, while the `getMeta` call the row named lands with the banner in T-08 (nothing in T-07 reads meta); T-07 adds a heading-only `/overview` page (the shell is then whole on its first link and after login) so US-01 AC1's Overview-heading assertion moves from T-10 to T-07; SPEC-auth v1.0.6 and an ADR-0006 amendment give a failed logout a fallback URL, `/login?reason=logout`, which the middleware answers by clearing the cookie for a same-origin navigation (US-03 AC2 could not hold otherwise — the cookie is `httpOnly` and §2.8 redirects a logged-in visitor away from `/login`); T-07 changes `middleware.ts`, so it also picks up **TD-1** ("the first task that changes `middleware.ts`"); the header's "Log out" icon is Phosphor "sign-out" (SPEC-app-shell v1.3, design-tokens v1.2 — nine shell tokens); hand-offs written into the T-08, T-10, T-11 and T-16 rows, as in v1.7–v1.9. v1.18 (2026-09-23, owner decisions on T-06's whole-branch review) — findings M1 and
M4 become tech debt TD-4 and TD-5 in `tech-debt.md` ("M1 və M4 tech dept əlavə olunsun"), each
named in the row of the task expected to pick it up (T-13, T-11). Finding M3 became a rule for
every page's document title, "Personal Finance - <page name>" (SPEC-app-shell v1.2 §2.5,
SPEC-auth v1.0.5); T-06 applies it to its pages, and the T-07 and T-10 rows carry it for theirs.
v1.17 (2026-09-23, owner decisions at the T-06 plan gate) — tech debt gets its own
file, `docs/03-specs/tech-debt.md` (owner: "Tech dept ayrıca fayl olsun. Backlogda bunun haqda
qeyd salanılsın. Əlaqə itməsin deyə."), and the Notes below link it. It opens with TD-1 — T-06
plan finding F2, the CSP nonce reaching Next through an undocumented header copy, kept as debt
rather than fixed before T-06 (owner: "F2 tapıntısı techdeptdə yazılır. Və backlogda
saxlanılır.") — and takes in two known items that had no home in this backlog: TD-2, T-05's
`middleware` → `proxy` deprecation (recorded only in the process log), and TD-3, F1's
`/_global-error` limitation. Copilot's review of PR #15 asked for the absolute home paths in one
verbatim prompt record to be redacted; the same paths are in 20+ committed process records, so
the decision is repository-wide and goes to T-16 (go-public hardening). v1.16 (2026-09-23, owner decisions at the T-06 plan gate, Q3/Q4) — US-03 AC1–AC2
(log out from the sidebar; back navigation does not reveal app pages; logout completes
client-side if the request fails) need the sidebar's logout button and the shell's `pageshow`
re-check, both T-07's, so their E2E moves from the T-06 row to the T-07 row; T-06 keeps US-03
AC3 (a demo reset ends the session and the login page says why). US-01 AC1's E2E can assert
only the URL and the session until Overview renders, so the T-10 row gets a hand-off to add the
Overview heading. E2E logs in per test with `loginViaApi` (ADR-0003 clarification
2026-09-23), which the T-07 row names. The prerendered 404 page that T-05's CSP breaks is fixed
by its own PR before T-06 (plan finding F1), not by a backlog task. v1.15 (2026-09-23, T-05's whole-branch review, finding C2, owner decision) — the
CSP nonce ADR-0006's T-05 plan gate had dropped as tech debt is restored (the premise it was
dropped on — "R1's App Router pages have no inline `<script>`" — was wrong; Next's own
RSC-payload data and inline styles are inline on every server-rendered page). `middleware.ts`
now forwards a fresh `x-nonce` per request; the T-06 row gets a hand-off note to read it via
`headers()` for any `<Script>` component, and to keep pages that need it on dynamic
rendering. v1.14 (2026-09-23, T-05's whole-branch review, finding I4) — T-05's own
`Cache-Control: no-store` API test could only assert the header's presence on a 404 (every R1
app page is still a 404 at this point in the backlog), and Next answers 404s `no-store` to an
anonymous request too — the test never isolated the middleware's own header from that default.
The T-07 row gets a hand-off note to add the real, isolating version once a genuine
authenticated 200 page exists. v1.13 (2026-09-23, owner decision at the T-05 plan gate, Q4) — T-05 needs the latest `ResetLog.at` for its resetEpoch check before T-08 runs, so `src/server/reset.ts` gains `latestResetAt(db): Promise<Date | null>` in T-05, not T-08; the T-08 row is updated to build `latestReset()` on top of it instead of re-querying. v1.12 (2026-09-23, review finding on PR #10) — T-05's login-401 hand-off gains a note: closing the body/status oracle (v1.11) doesn't by itself close a timing one, if schema validation short-circuits cheaply while the demo credential's compare does not; worth a check, not a blocker. v1.11 (2026-09-23, owner decision, security) — T-05's hand-off: `POST /api/auth/login` never sends 400 — a `LoginSchema` failure answers 401 `invalid_credentials`, identical to a genuinely wrong email/password, so the login endpoint gives no oracle for which field or failure kind a request hit (SPEC-auth v1.0.3). `POST /api/auth/signup` is unaffected. v1.10 (2026-09-23, owner decision on PR #10's Copilot review, finding 3) — T-05's row's 400 example corrected: `{ error: "validation", issues: toErrorIssues(error) }`, no `message` (SPEC-auth v1.0.2 — the client maps each issue's `path` + `code` to the copy appendix; API carries codes only). v1.9 (2026-09-23, owner decisions at the T-04 plan gate) — T-04 adds `zod` 4.6.5 as a dependency and writes `src/shared/enums.ts`, `copy.ts`, `schemas.ts`, `tool-schema.ts` and `test-ids.ts`; `src/server/http.ts` takes `ApiErrorCode` from the shared `ErrorEnvelope`; an ESLint rule keeps every `data-testid` in `test-ids.ts`; SPEC-auth v1.0.1, SPEC-app-shell v1.1 and user-stories v1.2 ride in the same PR. Every hand-off the T-04 plan addresses to a later task sits in that task's row, as in v1.7 and v1.8. v1.8 (2026-09-22, owner decisions at the T-03 plan gate) — T-03 also writes `src/shared/money.ts` and `src/shared/dates.ts` (the SPEC-overview §4.2 formatters), because `scripts/seed-figures.ts` prints §4.3 with them and T-03 runs first; T-04's scope narrows to the schemas, enums, copy and test ids. T-03 moves `toCents` and `shiftYears` from `src/server/seed.ts` into `src/domain` (ADR-0002 lets scripts import domain, not server), and narrows the ADR-0005 lint rule to the calls that read the wall clock (`new Date()` without arguments, `Date()`, `Date.now()`) so that `fixedClock` can build its date. The T-03 hand-offs for T-05, T-08, T-09, T-10 and T-13 sit in those rows, as in v1.7: T-05 separates system time from the business `Clock` (F3); T-13 narrows the coverage globs (F2); T-09's `BigInt` conversion moves to the repository edge. v1.6 (2026-09-22, owner decisions at the T-02 plan gate) — `GET /api/test/log` moves from T-02 to T-12, which writes the entries it returns; the CI API job (Postgres service) moves from T-05 to T-02, where the first database code lands, and T-05's API tests join it; T-13 drops the `package.json` overrides T-02 adds for the Prisma CLI's `deepmerge-ts` and `mysql2` once Prisma ships fixed versions. v1.4 (2026-09-22, owner decisions at the T-02a plan gate) — T-02a removes the stale `master` entry from the CI push trigger (the default branch is `main`), and T-13 keeps the rest of its CI work; T-16's "full-history secret scan as a blocking gate" now names the T-02a `secret scan` check and `npm run secrets:scan`, because a bare `gitleaks git` skips merge commits (43 of 49 commits on 2026-09-22). v1.2 (2026-09-20) — the 21 boundary fixtures import layer README stubs because `src/server`, `src/domain`, `src/shared` are empty; the task that fills each layer repoints its fixtures (was recorded only in `tests/fixtures/boundaries/README.md`); T-13 splits the CI concurrency group so `main` runs are never cancelled. v1.1 (2026-09-20) — new **T-02a secret guard** placed *before* T-02 because the first real `DATABASE_URL` lands in T-02 (proposed by the T-01 agent as part of T-02; split out by the owner to keep tasks one-session sized); new **T-16** hardens the repository before it is made public. v0.3 (2026-09-20) — T-05 deletes the T-01 placeholder page; T-13 install-script policy and `main`-only CI (T-01 hand-off). v0.2 — S-04/S-05 test-support and reset moved early; S-06 traceability scoped per release; S-07 CI from T-01; S-34 T-03/T-07 split; US-35 in R1.
Order is the intended execution order; each task is sized for one agent session and ends with the Definition of Done.

| Id | Task | Spec / ADR | Stories | Depends on |
|----|------|------------|---------|------------|
| T-01 | Scaffold: Next.js (App Router, TS strict), folder layout, ESLint boundaries, Prettier, Vitest, Playwright (3 engines) config, `tokens.css` + Public Sans via `next/font`, `public/avatars/` from the challenge assets, `.env.example`, README run instructions; **minimal CI** (install, lint, typecheck, unit) | ADR-0001/0002/0003, design-tokens | — | — |
| T-02a | **Secret guard** — the first real `DATABASE_URL` lands in T-02, so the guard exists before it: `.gitleaks.toml` extending the default rules with the `postgres_connection_string` case they miss, `.next/` and `docs/00-discovery/inputs/` allowlisted; pre-commit hook; CI `secret scan` job (`fetch-depth: 0`, fails on exit code; SARIF upload needs GitHub Code Security and is unavailable while the repo is private); `npm audit --audit-level=high` in CI; a fixture that proves the scan fires (DoD v1.1) | NFR-S5, ADR-0007 | NFR-S5 | T-01 |
| T-02 | Prisma schema + migrations; `prisma/data.json` copy with checksum test; `resetToSeed` (+2 y, cents, enums, avatar keys, `ResetLog`, advisory lock); `npm run db:reset`; Docker compose for local Postgres; **test-support routes** `/api/test/reset|seed` gated by `APP_ENV=test` with the absence unit test; **repoint the ADR-0002 boundary fixtures** that currently import `src/server/README.md` at the real modules this task adds (`tests/fixtures/boundaries/README.md`); **CI API job** (Postgres service, `API tests (Postgres)`) | SPEC-reset-and-test-support §2.1, 2.5, 2.7; ADR-0005 | US-36 | T-01, T-02a |
| T-03 | `src/domain` for R1: `Clock`, money helpers, `budgetSpent`, `latestTransactions`, `recurringBills` summary, `overviewSummary`; `scripts/seed-figures.ts` that prints SPEC-overview §4.3 from `data.json`; unit tests assert the generated figures (values **and order**); repoint the boundary fixtures that import `src/domain/README.md` at the real domain modules; **the SPEC-overview §4.2 formatters** `src/shared/money.ts` + `dates.ts` with their tests (from T-04); `toCents`/`shiftYears` move from `src/server/seed.ts` into `src/domain`; the ADR-0005 lint rule catches `Date()` and allows `new Date(<value>)`, with fixtures | SPEC-overview §4.2, §4.3, data-model; ADR-0005 | US-04…08 (logic) | T-01 |
| T-04 | `src/shared`: Zod schemas (auth, `OverviewDto`, `ErrorEnvelope`, meta), enums, `copy.ts` (appendix incl. R1 additions), `test-ids.ts`, (`money.ts` + `dates.ts` arrived in T-03); `toolInputJsonSchema` (`src/shared/tool-schema.ts`) for T-11's `defineTool` (plan finding F1); `src/server/http.ts`'s `ApiErrorCode` from the shared `ErrorEnvelope`; the ADR-0003 test-id lint rule with fixtures | NFR-Q2, SPEC-auth §2.10, SPEC-overview §6 | US-31 | T-01 |
| T-05 | Auth API: login/logout/signup/session routes, iron-session with `resetEpoch`, sliding re-issue, rate limit (`LoginAttempt`), middleware (route matrix, redirects, `no-store`), security headers, `X-Request-Id`; API tests; its API tests join the CI API job (T-02); **delete the temporary `app/page.tsx` from T-01** once middleware owns `/` (owner decision 2026-09-20); **from T-02:** index `LoginAttempt (ip, at)`; keep `/api/test/*` outside the session check and the rate limit; **from T-03:** keep **system time** (the session's sliding re-issue, the rate limit's window) apart from the **business `Clock`** (`src/domain/clock.ts`, fixed at 2026-08-19) — the plan names both, and injects system time into `src/server`, where the ADR-0005 rule forbids `new Date()`, `Date()` and `Date.now()`, rather than reading it there; **from T-04:** parse bodies with `LoginSchema`/`SignupSchema`; **login never sends 400** — a `LoginSchema` failure answers 401 `{ error: "invalid_credentials", message: "Email or password is incorrect" }` (`COPY.loginIncorrect`), identical to a genuinely wrong email/password, so the endpoint gives no oracle for which field or failure kind it hit (SPEC-auth v1.0.3, security) — a body/status oracle only; check whether cheap schema validation vs. a credential hash compare leaves a *timing* difference worth closing, given the single hardcoded demo credential (T-04 plan gate finding 3 security review, Minor); signup keeps 400 = `{ error: "validation", issues: toErrorIssues(error) }` — no `message`, `issues` are `{ path, code }` with `code` one of `required | invalid_format | too_short | too_long`, the client maps `path` + `code` to the copy appendix (SPEC-auth v1.0.2); 429 `message` "Too many attempts" with `retryAfter` equal to the `Retry-After` seconds; banner text `COPY.loginRateLimited(retryAfterMinutes(retryAfter))`; API tests parse bodies with the response schemas and `ErrorEnvelopeSchema`, which refuse unlisted fields | SPEC-auth §2.4, 2.8–2.10, §4, §6; ADR-0006 | US-01…03 | T-02, T-04 |
| T-06 | Auth UI: `(auth)` layout, `LoginForm`, `SignupForm`, `Field`, `PasswordField`, `Button`, demo box, reset notice; E2E for US-01/02, US-03 AC3 (AC1–AC2 move to T-07, v1.16) + keyboard + axe; **CI E2E job** (Chromium first); **from T-02:** seeding E2E tests share one database — run with one worker or one database per worker (decide in the Playwright config); **from T-04:** the forms validate with `LoginSchema`/`SignupSchema` and show each field's one issue, focusing the first; `EMAIL_MAX`, `NAME_MAX`, `PASSWORD_MAX` for `maxLength`; copy from `COPY`; test ids only from `TEST_IDS` (lint rule); **from T-05:** `middleware.ts` forwards a fresh CSP nonce per request via the `x-nonce` request header (ADR-0006, 2026-09-23 (2) amendment) — any `<Script>` component reads it with `(await headers()).get("x-nonce")` and passes it as the `nonce` prop; a page relying on it must render dynamically (it already does, once it reads the session) | SPEC-auth §2–3, §6 UI | US-01…03, US-31/32 | T-05 |
| T-07 | App shell part 1: `(app)` layout with the OT meta tag (the `getMeta` call lands with the banner in T-08, v1.19), sidebar (expanded/collapsed, US-35), bottom nav, page header, skip link, logout button; placeholder pages for the four R2 routes ("Coming in Release 2") and a heading-only Overview page that T-10 fills (v1.19); E2E for US-33/35 + US-32/34 rows + axe; **from T-04:** the shell's copy (`skipToContent`, `minimizeMenu`/`expandMenu`, `dismissNotice`, `comingInRelease2`) from `COPY`; **from T-05:** an authenticated real 200 page now exists — add the `Cache-Control: no-store` API test against it (T-05's own version, `tests/api/middleware.spec.ts`, could only prove the header on a 404, which Next answers `no-store` to an anonymous request too, so it never isolated the middleware's own header from Next's 404 default); **from T-06:** US-03 AC1–AC2 E2E — log out from the sidebar, back navigation does not reveal app pages (the shell's `pageshow` re-check, SPEC-auth §2.9), logout completes client-side if the request fails (§2.7); log in with `loginViaApi` from `tests/fixtures/e2e.ts` after the test's reset (ADR-0003 clarification 2026-09-23); each placeholder page's document title follows SPEC-app-shell §2.5 (v1.2): `metadata.title` is the page's `<h1>` text, and the root layout's template makes it "Personal Finance - <page name>"; **the logout fallback (v1.19):** when `POST /api/auth/logout` fails the client logs it and goes to `/login?reason=logout`, which `middleware.ts` answers — for a `Sec-Fetch-Site: same-origin` navigation only — by clearing `pf_session` and rendering the login page (SPEC-auth v1.0.6 §2.7–2.8, ADR-0006 amendment 2026-09-23 (3)); **from tech debt:** T-07 changes `middleware.ts`, so it fixes and closes **TD-1** (`tech-debt.md`) in the same change | SPEC-app-shell §2.1–2.5, 2.7–2.8 (v1.3); SPEC-auth §2.7–2.9 (v1.0.6); ADR-0006 | US-33, US-35, US-32, US-34, US-03 AC1–AC2 | T-05 |
| T-08 | App shell part 2: `getMeta` + `GET /api/meta`, reset banner with dismiss, `/api/admin/reset` route + Vercel cron config + threshold module (wired, not triggered in R1), session-ends-on-reset check; API tests; E2E for US-37 AC2; **from T-02:** implement `checkThreshold`; **from T-05:** `src/server/reset.ts` already exports `latestResetAt(db): Promise<Date \| null>` (the resetEpoch check) — build `latestReset()` here on top of it rather than re-querying; `ResetLog.at` is the app process's clock (Prisma runtime, not a column default); decide whether the threshold counts `ResetLog`/`LoginAttempt` rows; **from T-03:** the banner writes `formatDate(lastResetAt)` — three-letter months; **from T-04:** `GET /api/meta` answers `MetaDtoSchema`; the banner is `COPY.resetBanner(meta.resetIntervalDays, formatDate(meta.lastResetAt))` — the interval from `GET /api/meta`, never a literal 10; the admin reset body's `reason` is one of `RESET_REASONS` (T-08 writes that schema); **from T-07:** `app/(app)/layout.tsx` calls no `getMeta` — add it here with the `ResetBanner` above the page inside `Shell`; SPEC-app-shell §3's "meta unavailable → no banner" is the layout's `catch` | SPEC-app-shell §2.6, §5; SPEC-reset-and-test-support §2.2–2.6 | US-37 AC1/AC2, US-03 AC3 | T-07 |
| T-09 | Overview server + API: `getOverview`, `GET /api/overview`, DTO schema; API tests incl. 401, `no-store`, seed-variant shapes; **from T-02:** creation order is `seq`; `BigInt` money becomes `Number` at the repository edge, before `overviewSummary`; add overview-shape assertions per seed variant; **from T-03:** hand repository rows to `overviewSummary` — `BigInt` converted to `Number` first (`sumCents` refuses anything else), `seq` selected, `id`/`theme`/`avatar` passed through, `fixedClock(BUSINESS_TODAY)` as the clock; DTO dates via `toISOString()`; transactions and budgets must use the same category spelling — `budgetSpent` compares categories with `===`, and a mismatch shows only as $0 spent; `seedFigures()` rows keep data.json's avatar path, hex theme and display category name — map them (`avatarKey`, `themeFromHex`, `CATEGORY_BY_NAME` in `src/server/seed.ts`) or compare money, names and order only; **from T-04:** `GET /api/overview` answers `OverviewDtoSchema`, which is strict — map each row field by field (no `seq`; no `category` or `recurring` on transactions); categories and themes as data-model.md spells them, mapped in `src/server` from Prisma's identifiers with one map, tested against `CategorySchema.options` / `ThemeSchema.options`; avatar keys, not paths | SPEC-overview §6 | US-04…08 | T-03, T-04, T-05 |
| T-10 | Overview UI: stat cards, pots/transactions/budgets(donut)/bills cards, empty and error states; E2E for US-04 AC1, US-05…08 (incl. seed variants), US-32/34 rows; axe; **from T-03:** seed figures in E2E come from `seedFigures()` in `scripts/seed-figures.ts`, text through the `src/shared` formatters; `seedFigures()` rows keep data.json's avatar path, hex theme and display category name — map them (`avatarKey`, `themeFromHex`, `CATEGORY_BY_NAME` in `src/server/seed.ts`) or compare money, names and order only; **from T-04:** empty and error copy from `COPY`; any `data-testid` from `TEST_IDS`; **from T-07:** `app/(app)/overview/page.tsx` renders only `<PageHeader title={PAGE_NAMES.overview} />` and US-01 AC1's E2E already asserts the Overview heading (v1.19; it was T-06's hand-off to T-10) — fill the page's body; Overview's `metadata.title` is "Overview" (SPEC-app-shell §2.5 v1.2 → "Personal Finance - Overview") | SPEC-overview §2–4 | US-04…08, US-32, US-34 | T-07, T-09 |
| T-11 | WebMCP adapter: `adapter.ts` (modes, generation counter, readiness, `toolchange`), `defineTool`, `WebMcpProvider` + `WebMcpTools`, `AgentToolsStatus` with the four states, lazy loading, test hook; unit tests; **from T-04:** `defineTool` takes `inputSchema` from `toolInputJsonSchema` (`src/shared/tool-schema.ts`), which throws on an empty schema and on a string without `maxLength` (ADR-0004 as amended by the F1 PR); read `WEBMCP_MODE` with `WebMcpModeSchema`; indicator text from `COPY.agentTools*`, and its four `title`s (§2.7) join the copy appendix before `COPY`; **from T-06:** tech debt **TD-5** (`tech-debt.md`) — Zod's `jitless` (no eval probe under the CSP) is set by importing `src/shared/schemas.ts`; a client module that parses with Zod without it (e.g. `tool-schema.ts`) must import it or set `jitless` itself; **from T-07:** the indicator has two places — the sidebar footer above "Log out" (`src/ui/Sidebar.tsx`) and the page header's compact actions before the icon "Log out" (`src/ui/PageHeader.tsx`); `WebMcpProvider` wraps `Shell` in `app/(app)/layout.tsx` (`src/ui` may not import `src/webmcp`); **from T-08:** `Shell` receives `meta: MetaDto | null` from `app/(app)/layout.tsx` (`getMeta`, SPEC-app-shell §2.1) — the indicator reads `meta.webmcp.configuredMode` from it instead of fetching `/api/meta` again; `null` is "meta unavailable" (§3), and the indicator stays independent of it (SPEC-webmcp-tools §2.7); an unrecognised `WEBMCP_MODE` now fails the build in `next.config.ts` (PR #20 review), and an empty value is "polyfill" on both sides, so the client and `/api/meta` only ever see `native`, `polyfill` or `off` | SPEC-webmcp-tools §2, §6, §7 Unit; ADR-0004 | US-38, US-41 | T-07 |
| T-12 | R1 tools `get_balance`, `get_overview_summary`; via-marker logging + `/api/test/log` (moved from T-02); E2E in polyfill and off modes (`WEBMCP_MODE` matrix in CI); headed native runbook page; **from T-04:** tool output is the `OverviewDto` plus `{ currency, unit }`; validation errors as `toErrorIssues` | SPEC-webmcp-tools §3, §7, §2.8 | US-38, US-39, US-41 | T-10, T-11 |
| T-13 | CI hardening: Firefox + WebKit E2E, axe gate, coverage gate 90 % on `domain`, traceability script scoped to the release's story list (`docs/03-specs/release-1-stories.txt`, generated from PRD §5), PR template with the DoD checklist; **install-script policy**: evaluate `strict-allow-scripts=true` in `.npmrc` after checking the Linux native-binding set (T-01 hand-off, owner decision 2026-09-20); CI triggers on `main` only (the `master` push trigger was removed in T-02a, owner decision 2026-09-22); split the CI `concurrency` group so pushes to `main` never cancel an in-progress run — a follow-up merge currently discards the previous commit's verdict (PR #4); workflow `permissions: contents: read`; evaluate a commit/tag-message secret-scan pass (prototype from T-02a) **with its own failing fixture** before adopting; **drop the `deepmerge-ts`/`mysql2` `overrides`** (and their `"//"` note) from `package.json` once Prisma ships fixed versions — `npm audit --audit-level=high` must stay at 0 (T-02, owner decision 2026-09-22); **from T-02:** schema-vs-migrations drift check in CI; **from T-03:** narrow `vitest.config.ts`'s `coverage.include` to `**/*.ts` — it matches the layers' `README.md` files and prints parse errors on every coverage run; `src/domain` measured 100 % at T-03; **from T-04:** drop `vite-tsconfig-paths` for Vite's own `resolve.tsconfigPaths` — every Vitest run prints its deprecation notice (T-04 plan F3); **from T-06:** tech debt **TD-4** (`tech-debt.md`) — the WebKit CI job pins the "submit focused after an error" assertions; submitting those four tests with Enter closes it on Chromium too; **from T-12 (v1.21):** the E2E job is already a `webmcp-mode` matrix (`polyfill`, `off`; T-12) — Firefox and WebKit join as a second matrix dimension or a second job, decide there; the off leg's cost is one more build | ADR-0003/0007, DoD | NFR-T | T-06…T-12 |
| T-13a | **TD-2** (`tech-debt.md`): `middleware.ts` becomes `proxy.ts` through Next's `middleware-to-proxy` codemod; the matcher, the route matrix, the session and reset-epoch checks, the security headers and `X-Request-Id` behave as before; check what the `proxy` convention allows for `runtime` (TD-2 says keep `runtime: "nodejs"`); `buildCsp`'s one call site moves with the file (TD-6's note); every live reference follows — code comments, `tests/api/middleware.spec.ts`, READMEs, specs — while an Accepted ADR (ADR-0006) gets a dated note, not a rewrite; `next build` prints no `middleware` deprecation warning; API and all four E2E legs green; TD-2 closed | ADR-0006, `tech-debt.md` TD-2 | NFR-S2, S6 | T-13 |
| T-13b | **TD-3** (`tech-debt.md`): `/_global-error` is prerendered without the CSP nonce — find the fix TD-3 leaves open (a `global-error.tsx` without `style` attributes still leaves the prerendered inline scripts), within one session; a test pins the outcome — the page's inline tags carry the response's nonce, or, if Next 16.3.5 allows no fix, the measured reason goes into TD-3 for the owner to decide, instead of a forced change; reads T-13a's `proxy.ts`; **from T-13a (v1.30):** reads `proxy.ts`; `buildCsp(nonce, process.env.NODE_ENV)` is called from it as before and its development variant was read off a `next dev` response (T-13a, F7) | ADR-0006, `tech-debt.md` TD-3 | NFR-S6 | T-13a |
| T-13c | **TD-7–TD-11** (`tech-debt.md` v1.10–v1.11), one commit and one failing-first test each: **TD-7** `register()` pushes the cleared status to `onStatus` listeners before its tools settle; **TD-8** the keyboard login walkthrough's comment and its test agree (Shift+Tab or not — a test-design choice at the plan gate); **TD-9** a check that fails on a bare `fr` track in any `*.module.css` `grid-template-columns`, with a violation fixture (unit test or CSS lint rule, decided at the plan gate); **TD-10** `APP_ENV=test` refused on a deployment and `db:reset`/`test:api` refused against a non-local `DATABASE_URL` — not keyed on `NODE_ENV`, since CI and local API/E2E runs start a production build with `APP_ENV=test` (moved from T-14's row, "from T-02"); **TD-11** (v1.25) Public Sans served by `next/font/local` from committed `.woff2` files (400 and 700, latin, source and version recorded, the font's licence beside them), so no build downloads from Google Fonts; `design-tokens.md` §Typography follows (owner decision "a"); a check fails if `next/font/google` returns; **plan** `docs/04-process/plans/2026-09-24-T-13c.md` (merged as PR #41, 2026-09-25), **merged** as PR #39 on 2026-09-25 (`task/T-13c-tech-debt`, v1.27–v1.28) — decided at its plan gate: TD-8 is the comment, with no failing-first test (owner waiver), and TD-9 is a Stylelint rule | SPEC-webmcp-tools §2.2/§2.7, SPEC-auth §6, SPEC-app-shell, SPEC-reset-and-test-support, `design-tokens.md` §Typography, `tech-debt.md` TD-7–TD-11 | US-41, US-32, US-33, NFR-T3 | T-13 |
| T-13d | **Security review before the deploy** (owner decision 2026-09-24): the whole application and the repository, with the `owasp-security-review` skill (`.claude/skills/owasp-security-review/`, 131 items, its NFR-S1–S7 map) against a local production build — a review, not a fix. It also covers what the repository going public (2026-09-20) made current: (1) **NFR-S5** — the owner states that no secret was real before the flip (every value was local; Vercel's and Neon's are first created in T-14), recorded with its evidence: the full-history scan (2026-09-24: 366 commits and every commit and tag message, no leaks; no `.gitleaksignore` ever) re-run with `--ignore-gitleaks-allow`; (2) **the GitHub side** — the `main` ruleset (today it requires a pull request, blocks force-push and deletion, and blocks on a high CodeQL alert, but requires no status check and no approval), CODEOWNERS for the guard files, `SECURITY.md` (private vulnerability reporting is on), secret scanning's non-provider patterns and validity checks (off), third-party actions pinned by tag rather than SHA, and what a fork's pull request can run; (3) TD-10's guard as T-13c built it — **from T-13c's final review (v1.27), to check, not measured here:** the stock Prisma commands that reset a database are not guarded — `prisma migrate reset`, `prisma db push --force-reset`, `migrate dev`'s reset prompt — since `prisma.config.ts` loads `.env.local` and nothing refuses outside `npm run db:reset` (read in the file); `migrate reset` resets the database and applies the migrations but does not run `prisma/seed.ts` (Prisma 7 no longer seeds automatically; only `db seed` calls the seed runner), so on a non-local URL it ends with an empty database — schema re-created, no seed rows; `migrate reset` and `migrate dev`'s reset ask for confirmation, `db push --force-reset` does not (all of this read from the installed CLI's source, Prisma 7.10.0 `node_modules/prisma/build/cli.js`, by the whole-branch review's second pass, and not run); low impact (demo data, and none of the three is a project script); (4) dependencies — `npm audit`, the `overrides`, the install-script policy, `stylelint` (dev), added by T-13c (v1.27). **Disclosure (owner decision):** an exploitable finding is recorded in a private GitHub security advisory draft until its fix is merged; the report and the process log carry it after the fix, and a report with no such finding is committed directly. Each finding is fixed by a small PR of its own with a failing test first (the skill's rule), or becomes a TD entry by owner decision. Which of T-16's go-public items leave T-16 is decided with the release-strategy change (owner, next session), not here; **from T-13a (v1.30):** the review reads `proxy.ts` — the "auth middleware" of the OWASP checklist's AUTHZ-02, reworded in T-13a — and `tests/api/proxy.spec.ts`, which now pins `Referrer-Policy`, `X-Content-Type-Options` and `X-Request-Id` on four branches and the matcher's file-extension exclusion; `src/server/request-log.ts`'s `globalThis` buffer is the one place the code shares a global between the proxy and the route handlers, which Next's proxy documentation advises against — it is test-only and proven under `next start` (T-13a, F11a) | NFR-S1–S7, ADR-0006, ADR-0007, `.claude/skills/owasp-security-review/` | NFR-S1–S7 | T-13a, T-13b, T-13c |
| T-14 | Deploy: Vercel project, Neon main + preview-branch workflow, env vars, first `resetToSeed`, cron verified, Lighthouse CI on a warm instance, `docs/04-process/runbooks/deploy.md` (OT token, native check, relay demo, secret rotation); **from T-02:** ~~startup guard so `APP_ENV=test` can never run in production~~; check whether `prisma migrate deploy` needs Neon's direct (non-pooled) URL; ~~guard `test:api`/`db:reset` against a non-local `DATABASE_URL`~~ — both guards moved to T-13c as TD-10 (v1.24); confirm on the deployment that TD-10's guard refuses `APP_ENV=test` there; **from T-13c (v1.27):** the first seed is `POST /api/admin/reset` with `RESET_SECRET` and an empty body (reason `manual`), after `prisma migrate deploy`; `npm run db:reset` refuses a non-local `DATABASE_URL` before it applies any migration (SPEC-reset-and-test-support v1.6, the owner's choice "B"), while the direct `npx prisma migrate deploy` that T-14 runs against Neon is not guarded, by design; the guard's `VERCEL` line needs the Vercel project's 'Enable access to System Environment Variables' setting on, its database line does not — read the setting; **also from T-13c's final review (v1.27), unmeasured:** check whether the Vercel CLI's `vercel env pull` writes `.env.local` by default (the whole-branch review raised it) — if it does, the pull replaces the local settings with Neon's URL plus `VERCEL`/`VERCEL_ENV`, so `npm run db:reset`, `test:api` and `test:e2e` all refuse (as intended, with a message about a Vercel deployment or another machine's database; `db:reset` before it applies any migration); **from T-08:** set `CRON_SECRET` (≥ 16 random characters) on the Vercel project alongside `RESET_SECRET` — ADR-0007's env list omits it, and without it Vercel's cron `GET` carries no `Authorization` header and every scheduled reset answers 401 (vercel.com/docs/cron-jobs, read 2026-09-23; SPEC-reset-and-test-support v1.3 §2.3); verify it against the deployed `/api/admin/reset` — on Hobby the call lands within the scheduled hour, UTC, and redirects are not followed; **from T-12 (v1.21):** `deploy.md` links to `docs/04-process/runbooks/webmcp-native-check.md` (T-12) for the headed native check instead of duplicating it; **from T-13 (v1.22):** Vercel's `npm ci` reads `.npmrc`, whose `strict-allow-scripts=true` needs npm >= 11.19 — now in `package.json` `engines` and the README — on Vercel's Node 26 image; an older npm only *warns* `Unknown project config` and does not enforce the install-script policy, so read Vercel's install log for that line and confirm the npm version; also confirm that `fsevents` (absent on Linux, denied in `allowScripts`) is tolerated there (T-13 measured `npm ci` on the `node:26` image, npm 11.19.1, exit 0, and the policy enforced — Vercel's own image was not tried); **from T-13a (v1.30):** the first preview deployment is where `proxy.ts` first runs on Vercel — check that `/` answers 302 to `/login` without a session and that the security headers and the nonced CSP come back on every response; a "Unable to find source file for page middleware" deploy warning is a known false positive that Vercel's CLI stopped printing on 2025-10-30 (Vercel Community, read 2026-09-25); nothing about `proxy.ts` on Vercel was measured in T-13a | ADR-0007 | NFR-D4/D5, P1 | T-13d |
| T-15 | Release 1 retrospective entry: what the specs missed, what agents got wrong, template/AGENTS.md changes; Release 2 spec work opened; **from T-04:** T-02 D5 left name lengths and amount ranges (NFR-S3) to the shared schemas — Release 2's write schemas carry them in `src/shared/schemas.ts` (ids with `.max(36)` for `toolInputJsonSchema`); the amount copy is already in `COPY`; **from T-08:** `checkThreshold` (`src/server/threshold.ts`) has no call site in Release 1 — Release 2's write endpoints call it after each successful write and, when it is exceeded, `resetToSeed(db, "threshold")` and answer 409 `{ error: "conflict", message: "Data was reset" }` (SPEC-reset-and-test-support §2.4, US-37 AC3); each call runs three counts and `pg_database_size`, so consider a cheaper or sampled check (PR #20 review, finding 7); **from T-12 (v1.21, Release 2 tools):** through the polyfill, `getTools()` drops `consequentialHint` (T-12 plan F3) and a tool's `execute` never receives a runtime `AbortSignal` (T-11 Q2) — assert the `consequentialHint` rule in the registry unit test, and drive "cancel" through the dialog, not through a signal; **from T-13 (v1.22, retro):** T-12 merged with `npm run test:all` red on Firefox and WebKit (plan finding F1) while its DoD line was ticked — a DoD gap; add "the PR lists the result of all three engines" to the reviewer's checklist (CI now runs them, T-13 Task 10, but a local result is still what the PR must quote until CI has proved Linux); the coverage table Vitest prints hides files that are 100 % covered, so read `json-summary` when a gate is in doubt; the way to prove a threshold fails is a fixture config with its own tiny `src/domain` (`tests/fixtures/coverage-gate/`), run as a nested Vitest; the per-task reviews of T-13 led to a fix in nine of the ten reviewed tasks (Tasks 2–10; Task 1's one minor was deferred), and every Important finding of a first review round — Tasks 3, 5 and 8, and PR-A's Task A1 — sat in code or text the plan itself dictated (`docs/04-process/process-log.md`, T-13 execution entry); that a plan's "Expected" lines are predictions and must be labelled as such is a template question; **from the ADR-0003 acceptance (v1.23, owner condition):** when Release 2 spec work opens, the release's story list is regenerated for Release 2 (a `release-2-stories.txt` from PRD §5's Release 2 `Stories:` sentence) and `scripts/traceability.ts` reads the release being built — today it names "Release 1", its list file and PRD §5's `### Release 1` heading in code (T-13 review, deferred minor) | roadmap Phase 5 exit | S4 | T-14 |
| T-16 | Go-public hardening: full-history secret scan as a blocking gate — make the T-02a `secret scan` check required in the `main` ruleset and scan with `npm run secrets:scan` (a bare `gitleaks git` skips merge commits); **rotate every secret that was ever real** (`SESSION_SECRET`, `RESET_SECRET`, `CRON_SECRET`, Neon `DATABASE_URL`, OT token) before the flip — no scanner covers generic high-entropy strings, so rotation is the guarantee rather than the scan; make the repository public; then enable GitHub secret scanning + push protection, ~~CodeQL default setup (JS/TS)~~ — replaced 2026-09-24 by the advanced workflow `.github/workflows/codeql.yml`, suite `security-and-quality`, JavaScript/TypeScript and Actions (owner decision, PR #37, v1.26), Dependabot alerts and security updates, and a `main` ruleset requiring one human approval with Copilot approvals off; README attribution per `research/frontend-mentor-license.md` (challenge link, "Challenge by Frontend Mentor. Coded by …" footer, design not included) and a licence file for our own code with the challenge-assets clause; ~~**remove the Claude Design exports (`docs/00-discovery/inputs/design/*.html`) from the tree and from history** before the flip and update that folder's README~~ — **done 2026-09-20** (see process log); run the full-history scan with `--ignore-gitleaks-allow` so inline allow comments cannot silence it; protect the guard files (`.gitleaks.toml`, the secret-scan workflow, the pre-commit hook) with CODEOWNERS or a ruleset requiring owner review; **from PR #15's review (2026-09-23):** decide, once for the whole repository, whether the absolute home-directory paths (`/Users/<name>/…`) in the committed plans and verbatim prompt records (20+ files under `docs/04-process/` by T-06) are redacted before the flip — not file by file, since the prompt records are verbatim; **from T-07 (v1.19):** the licence file's third-party notices include Phosphor Icons (MIT, © 2023 Phosphor Icons) for `src/ui/icons/SignOutIcon.tsx`; **from T-08 (v1.20):** the licence notes record `src/ui/icons/CloseCircleIcon.tsx`'s source — the Claude Design prototype's own inline close control (`closeCircle`), not a Phosphor file and not a Frontend Mentor asset (design-tokens v1.3); **from T-12 (v1.21):** list both `E2E (Chromium, polyfill)` and `E2E (Chromium, off)` among the required checks when the repository becomes public and protection becomes available; **from T-13 (v1.22):** the required checks are now `E2E (Chromium, polyfill)`, `E2E (Chromium, off)`, `E2E (Firefox, polyfill)`, `E2E (WebKit, polyfill)`, `lint · typecheck · unit` (which also runs the coverage gate and the traceability check), `API tests (Postgres)` (which also runs the drift check) and `secret scan` (which now also reads commit and tag messages; a `commit-msg` hook that would read the message being written is not built); `npm audit` is non-blocking by design and stays out of the list; **the `deepmerge-ts`/`mysql2` `overrides` removal lands here (owner: T-16, Q7):** re-measured 2026-09-24 — without the overrides `npm audit` reports 4 high (`prisma@7.10.0`, the newest stable 7.x, pins `deepmerge-ts` 7.1.5 through `@prisma/config` and `mysql2` 3.15.3; the `latest` dist-tag is `8.0.0-rc.15`, a release candidate) — so remove both overrides and their `"//"` note only once a scratch copy without them audits clean (`npm install --package-lock-only --ignore-scripts`, `npm audit --audit-level=high`); **the first CI run of the T-13 workflow is the evidence for what was only measured on macOS or offline** — read it for: (1) the four E2E legs green on Linux (Firefox and WebKit were run only on macOS; WebKit's `tabTo()` Alt+Tab path and TD-4's four focus tests are the Linux-sensitive ones); (2) no `Unknown project config` warning in the `verify` job's install log, and `install-scripts.test.ts` case 2 passing on the runner — the runner's npm is whatever ships with Node 26.x (`.nvmrc` is `26`), `strict-allow-scripts` was proved enforcing on `node:26`, npm 11.19.1, not on the runner; (3) the `concurrency` behaviour — a push to a pull request cancels the older run, two merges to `main` in a row keep both verdicts; (4) no job failing for want of write access under `permissions: contents: read` (since v1.23 every job's checkout, `verify`'s included, sets `persist-credentials: false`); (5) `E2E (Chromium, off)` has never run T-13's new specs (the 8 `axe-routes` tests, the four Enter-submit tests, PR-A's failed-registration test) — only Firefox off was measured (106 passed, 11 skipped, macOS) — so a red Chromium-off leg is not necessarily a Linux problem; (6) `tests/api/schema-drift.spec.ts` first runs against the CI Postgres service container (`postgres:18.6-alpine`, `--from-config-datasource`), measured so far only against the local compose database; (7) the message scan on the runner reads every ref, `refs/remotes/origin/*` and tags included (all remote branches such as `worktree-t-13-plan` and `docs/T-06-plan-v0.3`, and the pull request's merge commit), where locally only local refs were measured; **from T-13c (v1.27):** the third-party notices include Public Sans (SIL OFL 1.1, © 2015 The Public Sans Project Authors) in `app/fonts/`, text in `app/fonts/OFL.txt`, source in `app/fonts/README.md` | ADR-0007, governance.md, DoD, `research/frontend-mentor-license.md` | NFR-S5 | T-14, T-15 |
Notes
- T-02a precedes T-02; T-03 and T-04 run in parallel with T-02; T-06/T-07 after T-05; T-11 after T-07; T-16 is the last task of Release 1 and precedes any public announcement.
- T-13a–T-13d run after T-13 and before T-14 (v1.24, owner decision 2026-09-24; T-13c gains TD-11 in v1.25): T-13b after T-13a, since both touch the CSP path; T-13c is independent of both; T-13d is last, so the security review reads the code T-14 deploys.
- Deferred to Release 2 by owner decision: US-04 AC2, US-37 AC3.
- **Tech debt** lives in [`tech-debt.md`](tech-debt.md) (v1.17, owner decision 2026-09-23): known shortcuts kept for now, each with an id (`TD-n`), its guard, its fix and the task expected to pick it up. Before a task changes a file an entry names, it reads the entry; the task that fixes one marks it Closed there. Open at v1.18: **TD-1** (the CSP nonce reaches Next through an undocumented header copy — `middleware.ts`; picked up by T-07, v1.19), **TD-2** (`middleware` → `proxy` file convention), **TD-3** (`/_global-error` prerendered without the nonce), **TD-4** (two "submit focused after an error" E2E assertions prove nothing on Chromium — T-13), **TD-5** (Zod's `jitless` rides on importing `schemas.ts` — T-11/T-12). Open at v1.24: **TD-2** (T-13a), **TD-3** (T-13b), **TD-7–TD-10** (T-13c). Open at v1.27: **TD-2** (T-13a), **TD-3** (T-13b); **TD-7–TD-11** in review (T-13c). Open at v1.30: **TD-3** (T-13b); **TD-2** in review (T-13a).
- Every PR follows `definition-of-done.md`; the owner merges. `release-1-stories.txt` = US-01, 02, 03, 04, 05, 06, 07, 08, 31, 32, 33, 34, 35, 36, 37, 38, 39, 41.
