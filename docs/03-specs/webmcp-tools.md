# SPEC-webmcp-tools — Adapter, registries, Release 1 tools, status indicator

Status: **Approved** (v1.0.5 — **proposed by the agent, awaiting the owner's approval** — 2026-09-24: §2.2 says the polyfill needs `Origin-Agent-Cluster: ?1` and that a rejected registration is reported, §2.7's `unavailable` row adds "every tool registration of the page was rejected", §6 and §7 follow (T-13 plan findings F1–F2, owner decision Q2); v1.0.4 — 2026-09-24: §7's E2E call shape, §2.3's wrapper, §2.5's fetch wrapper, §2.6's `issues`, §2.8's 401 and 404 rows and §3's flat output corrected to what the installed runtime and the built code do (T-12 plan gate, owner decisions Q1–Q2); v1.0.3 — 2026-09-24: §2.2 corrects `unregisterAll()` — the installed `@mcp-b/webmcp-polyfill@5.1.0` exposes no `unregisterTool(name)` (T-11 plan gate, finding Q1); v1.0.2 — 2026-09-23: §2.4 names `z.toJSONSchema`, not `zod-to-json-schema` (T-04 plan gate, finding F1); v1.0.1 — 2026-09-22: §2.8 cross-reference corrected; v1.0, owner approval 2026-09-20) · Author(s): Agent · Date: 2026-09-20
Changelog: v1.0.5 (**proposed by the agent, awaiting the owner's approval** — 2026-09-24, T-13 plan findings F1–F2, owner decision Q2) — §2.2: the installed
polyfill (`@mcp-b/webmcp-polyfill@5.1.0`) throws `SecurityError` from `registerTool`, `getTools` and
`executeTool` unless `originAgentCluster` is true, and Firefox and WebKit report `false` for a
document served without `Origin-Agent-Cluster: ?1`, so the app sends that header (ADR-0006
amendment (5), proposed). A `registerTool` rejection used to pass silently, leaving `ready` and an
indicator of `polyfill · 0`; §2.2 now has the adapter `console.warn` once per rejected tool and set
`data-webmcp-error`, and §2.7's `unavailable` state also covers "every tool registration of the
page was rejected" (owner decision on T-13 plan Q2: the page must say WebMCP is not reachable;
`data-webmcp="ready"` stays), which also means `mode()` (and so `onStatus` and the `window.__pf`
hook, §6) reports `unavailable` in that state. §7 lists the new unit and E2E assertions. v1.0.4 (2026-09-24, T-12 plan gate) — §7 E2E: against the installed polyfill (5.1.0) a test
finds the tool in `document.modelContext.getTools()` and calls
`executeTool(tool, JSON.stringify(input))`, which returns a JSON string of the whole result; the
`executeTool("get_balance", {})` spelling does not exist outside the deprecated
`navigator.modelContextTesting` shim. `getTools()` shows only `readOnlyHint` and
`untrustedContentHint`; the full annotation rule (NFR-W3) is asserted by the registry unit test.
§2.3: the page's layout renders a no-prop `"use client"` wrapper that owns the registry
(`<OverviewTools />`, `src/webmcp/tools/`) rather than `<WebMcpTools tools={pageTools}/>` itself —
`pageTools` holds `execute` functions, which a Server Component cannot pass to a Client Component.
§2.5: the fetch wrapper is `src/shared/api-client.ts` (`apiGet`, returning a tagged outcome); the
Release 1 tools pass it `X-Via: webmcp` and — while the runtime hands a tool no signal (T-11 Q2) —
no abort signal (the wrapper accepts one), so the `aborted` → `cancelled` mapping is exercised by
unit tests only. §2.6: a `validation` error also carries `issues` (`{ path, code }[]`, from `toErrorIssues`, or
copied from a 400 body) when the shared contract can name them, and a `rate_limited` error carries
`retryAfter` when the API gave one; a tool with no input ignores unknown keys. §2.8: the marker is recorded by `middleware.ts` before the session check, so a 401
is on record; `GET /api/test/log` answers 404 for an unknown, empty or missing `requestId`. §3:
`get_balance`'s output is the flat balance object plus `{ currency, unit }` (and §7's unit row now names the `issues`/`retryAfter` assertions). v1.0.3 (2026-09-24, T-11 plan gate, finding Q1, owner accepted "as recommended") — §2.2: `register`/`unregisterAll` no longer describe a name-based `unregisterTool(name)` call. The installed `@mcp-b/webmcp-polyfill@5.1.0`'s own compatibility-boundary section states plainly that `unregisterTool()`, `provideContext()` and `clearContext()` "are not exposed" — a tool's whole lifetime is instead tied to the `AbortSignal` passed to `registerTool(tool, { signal })` at registration (`dist/index.js`, read directly: `signal?.throwIfAborted()` before a tool is added, an `abort` listener removes it). `register(tools)` now creates one `AbortController` per generation and passes its `signal` to every `registerTool` call in the batch; `unregisterAll()` is exactly that controller's `.abort()` — which both cancels a still-pending registration (the signal is already aborted when `registerTool` checks it) and removes every already-registered tool of that generation, in the one call. This reads the same as the prior text everywhere else: one generation is "current" at a time, and `unregisterAll()` still removes everything the last `register()` added. v1.0.2 (2026-09-23, T-04 plan gate, finding F1) — §2.4: `defineTool` converts with Zod 4's own `z.toJSONSchema(schema, { io: "input" })`; `zod-to-json-schema` returns an empty schema for a Zod 4 object with no error (evidence E7/E17, `docs/04-process/plans/2026-09-23-T-04.md`). v1.0.1 (2026-09-22, T-02 plan gate) — §2.8 pointed at SPEC-reset-and-test-support §3 (States); the test-support routes are §2.7. v0.2 — S-08 cents in tool output; S-09 env names; S-10 indicator states; S-20 observable via-marker; S-21 `toolchange` rule; S-22 registration generation; S-23 assertions defined; S-31 names aligned with ADR-0004; S-32 page-scope in descriptions; S-33 idle fallback; S-37 execute signature.
Implements: US-38, US-39 (R1 part), US-41; reserves names for R2 · Constrained by: ADR-0004, NFR-W1–W9, NFR-T8 · Spec reference: WebMCP Draft CG Report 2026-09-04 — `document.modelContext`; `execute(input, signal)` is the draft's callback shape (research note F2); the adapter wraps whatever the runtime passes and exposes `{ input, signal }` to our tools.

## 1. Purpose
Expose the app's capabilities to in-browser agents through one adapter, page by page, with the polyfill as baseline, and make it testable without any agent.

## 2. Behaviour
2.1 Configuration: the documented knob is `WEBMCP_MODE` (`native | polyfill | off`, default `polyfill`); `next.config` exposes it to the client as `NEXT_PUBLIC_WEBMCP_MODE`. `native` means *opportunistic native with polyfill fallback* (there is no way to force native). `APP_ENV=test` is exposed as `NEXT_PUBLIC_APP_ENV` for the test hook.
2.2 `src/webmcp/adapter.ts` (names per ADR-0004): `getModelContext()`, `register(tools)`, `unregisterAll()`, `mode()`, `onStatus(cb)`.
  - `off` → `register` no-op; `mode() = "unavailable"`.
  - The polyfill throws `SecurityError` unless `originAgentCluster` is true; the app sends `Origin-Agent-Cluster: ?1` (ADR-0006 (5)) (v1.0.5).
  - Otherwise: if `"modelContext" in document` before the app installs anything → `mode = "native"`; else `await import("@mcp-b/webmcp-polyfill")` once → `mode = "polyfill"`; import failure → `mode = "unavailable"`, `console.warn`.
  - `register(tools)` creates one `AbortController` per generation, awaits the polyfill if needed, then calls `document.modelContext.registerTool(def, { signal })` for each tool with that generation's signal — a still-current generation, since the signal check inside `registerTool` (the runtime's own) rejects a tool whose signal was already aborted, giving "only if the generation is still current" without a separate counter comparison (v1.0.3, T-11 plan Q1). `unregisterAll()` aborts that controller, which cancels any pending registration and removes every already-registered tool of that generation in the one call — there is no name-based `unregisterTool` on the installed runtime.
  - After a completed `register`: set `document.documentElement.dataset.webmcp = "ready"`, dispatch **one** `toolchange` `Event` on `document` and, if `document.modelContext` is an `EventTarget`, on it too (duplicates with runtime-fired events are harmless; tests wait only on the attribute). `unregisterAll` removes the attribute.
  - When any `registerTool` call is rejected the adapter `console.warn`s once per rejected tool (`[webmcp] could not register tool "<name>"`, then the reason object) and sets `document.documentElement.dataset.webmcpError` to `<tool>: <reason>` pairs joined by `; ` (removed by `unregisterAll` and at the next `register`); `<reason>` is the error's `name`, followed by `: ` and its `message` when it has one (`SecurityError`, `Error: duplicate name`), else `String(reason)`. `data-webmcp` is still `ready`. A superseded generation (one whose `AbortSignal` was aborted by a later `register` or by `unregisterAll`) stays silent: no warning and no attribute. While every tool of the current generation was rejected (a page that registers no tools is not a failure), `mode()` — and so `onStatus` and `window.__pf.webmcp.mode()` — reports `"unavailable"` until the next `register` or `unregisterAll` (v1.0.5).
  - Loading: adapter and polyfill chunks load after hydration via `typeof requestIdleCallback === "function" ? requestIdleCallback(cb, { timeout: 2000 }) : setTimeout(cb, 0)` (NFR-P3).
2.3 `src/webmcp/WebMcpProvider.tsx` (client) is mounted once in `app/(app)/layout.tsx` and owns the adapter and the indicator state; each page's own `layout.tsx` renders a no-prop `"use client"` wrapper (`<OverviewTools />`, `src/webmcp/tools/`) that imports the page's registry and renders `<WebMcpTools tools={pageTools}/>` — a Server Component cannot pass `execute` functions to a Client Component as props (v1.0.4); `<WebMcpTools>`'s effect calls `register(tools)` on mount and `unregisterAll()` on unmount (React remounts it per route segment because it lives in the page's own `layout.tsx`, not the shared one). Pages without tools (R2 placeholders) render no `<WebMcpTools>`, so `mode()` is still reported with 0 tools. Nothing is mounted in `(auth)`.
2.4 `src/webmcp/tools/<page>.ts` exports `ToolDefinition[]` built with `defineTool({ name, title, description, input: ZodSchema, annotations, execute })`; `defineTool` converts Zod → JSON Schema with `z.toJSONSchema(schema, { io: "input" })` (Zod 4; its default `reused: "inline"` behaviour needs no `$refStrategy`-equivalent option), and throws at build time if: name ∉ `^[A-Za-z0-9_.-]{1,128}$`, description > 200 chars, any string property lacks `maxLength`, or annotations are missing.
2.5 `execute({ input, signal })`: validates `input` with the Zod schema → `error("validation")`; calls `src/shared/api-client.ts` (`apiGet`, a fetch wrapper that returns a tagged outcome and never throws) with header `X-Via: webmcp` (the wrapper (`apiGet`) accepts a signal; the Release 1 tools do not pass one (the runtime hands a tool none, T-11 Q2); v1.0.4); maps responses: 2xx → success; 400 → `validation`; 401 → `unauthenticated`; 404 → `not_found`; 409 → `conflict`; 429 → `rate_limited`; abort → `cancelled`; other → `server_error`. Never throws.
2.6 Result shapes (ADR-0004): success `{ content: [{ type: "text", text: JSON.stringify(data) }], structuredContent: data }` where `data` is the API DTO **unchanged (money in integer cents)** plus `{ currency: "USD", unit: "cents" }` at the top level; error `{ isError: true, code, message, content: [{ type: "text", text: message }] }`; a `validation` error also carries `issues: { path, code }[]` (from `toErrorIssues`, or copied from a 400 body) when the shared contract can name them, and a `rate_limited` error carries `retryAfter` when the API gave one; a tool with no input ignores unknown keys (v1.0.4).
2.7 Indicator `<AgentToolsStatus>` (sidebar footer; compact dot on tablet/mobile with `aria-label` = full text), `role="status"`, `aria-live="polite"`:

| State | When | Text | `title` |
|-------|------|------|---------|
| checking | before the adapter resolves | "Agent tools: checking…" | "Detecting WebMCP support" |
| native | `mode() === "native"` | "Agent tools: native · N" | "WebMCP is supported natively by this browser" |
| polyfill | `mode() === "polyfill"` | "Agent tools: polyfill · N" | "Provided by a polyfill; no built-in agent yet" |
| unavailable | off, import failed, **or every tool registration of the page was rejected (at least one tool)** | "Agent tools: unavailable" | "WebMCP is disabled or could not load" |

N = number of currently registered tools (0 on pages without tools).
2.8 Via-marker observability: every API response carries `X-Request-Id`; requests with `X-Via: webmcp` are logged as `{ requestId, via: "webmcp", route }` — recorded by `middleware.ts` before the session check, so a 401 is on record too; when `APP_ENV=test`, `GET /api/test/log?requestId=` returns that entry, and answers 404 for an unknown, empty or missing `requestId` (SPEC-reset-and-test-support §2.7; v1.0.4).

## 3. Release 1 tools (Overview page)
| Tool | Title | Description | Annotations | Input | Output (`structuredContent`) | Calls |
|------|-------|-------------|-------------|-------|------------------------------|-------|
| `get_balance` | Get balance | "Returns the demo account's current balance, income and expenses in USD cents. Available on the Overview page." | `readOnlyHint: true` | `{}` | `{ current: 483600, income: 381425, expenses: 170050, currency: "USD", unit: "cents" }` (the flat balance object plus `{ currency, unit }`; v1.0.4) | `GET /api/overview` (balance part) |
| `get_overview_summary` | Get overview summary | "Returns what the Overview page shows: totals, first four pots and budgets, latest five transactions, bills summary. Money in USD cents. Available on the Overview page." | `readOnlyHint: true`, `untrustedContentHint: true` | `{}` | `OverviewDto` + `{ currency, unit }` | `GET /api/overview` |

## 4. Reserved names (Release 2, defined in their page specs)
`list_transactions`, `list_budgets`, `add_budget`, `edit_budget`, `delete_budget`, `list_pots`, `add_pot`, `edit_pot`, `delete_pot`, `add_money_to_pot`, `withdraw_from_pot`, `list_recurring_bills`. Delete flow per ADR-0004 (`ConfirmDeleteDialog` via `src/webmcp/bus.ts`; results `cancelled | busy | not_found`).

## 5. Data
No storage. Request log entries as in 2.8 (in-memory ring buffer of 200 in test mode; structured stdout otherwise).

## 6. Interfaces
Types in `src/webmcp/types.ts` mirror the draft: `ToolDefinition { name, title?, description, inputSchema, annotations?, execute }`. Test hook: `window.__pf.webmcp = { mode, tools }` only when `NEXT_PUBLIC_APP_ENV === "test"`; `mode()` is the adapter's, so it reports `"unavailable"` while every registration of the current generation was rejected (§2.2, v1.0.5) — a test that reads it to tell which build it is talking to can see that instead of `"polyfill"`.

## 7. Tests required
| Level | What is asserted | Traces to |
|-------|------------------|-----------|
| Unit (jsdom) | mode detection native/polyfill/unavailable; register/unregister counts; stale-generation registration dropped after `unregisterAll`; readiness attribute set/removed; single `toolchange` dispatch; a rejected registration (v1.0.5, `adapter.test.ts`): every tool rejected → one warning per tool, `data-webmcp` still `ready`, `data-webmcp-error` names each tool and reason, `mode()` and the last `onStatus` value are `unavailable` with count 0; only some rejected → mode and count stay honest and the error names only the broken tools; `unregisterAll`, and the next `register`, clear the error and the unavailable state; a superseded generation's late rejection neither warns nor sets the error; a page with no tools is not a failure; `defineTool` rejects bad names, long descriptions, strings without `maxLength`, missing annotations; every registry tool has required annotations; `execute` maps 400/401/404/409/429/abort/network and never throws; a `validation` error carries `issues` and a `rate_limited` error carries `retryAfter` (`tool-result.test.ts`, `defineTool.test.ts`) | 2.2, 2.4–2.6, NFR-W3 |
| API | responses carry `X-Request-Id`; `X-Via: webmcp` request appears in `GET /api/test/log` (also when the request answers 401; 404 for an unknown, empty or missing `requestId`) | 2.8 |
| E2E (polyfill) | on `/overview`: `await page.waitForSelector('[data-webmcp="ready"]')`; `getTools()` names = exactly the two R1 names with the annotations `getTools()` shows (`readOnlyHint`, `untrustedContentHint`; the full NFR-W3 rule is the unit test's); the `get_balance` call — find the tool in `getTools()`, `executeTool(tool, JSON.stringify({}))`, parse the returned JSON string — has structuredContent equal to the API DTO balance + currency/unit; the same call for `get_overview_summary` equals `GET /api/overview` + currency/unit; with the cookies cleared a call returns `unauthenticated` and no data (US-39 AC4); a tool call's API request carries `X-Via: webmcp` and is on record at `GET /api/test/log` (§2.8); click sidebar "Transactions" (client navigation) → `[data-webmcp]` absent and `(await document.modelContext.getTools()).length === 0`; hard-load `/login` → `document.modelContext === undefined`; indicator text "Agent tools: polyfill · 2" on Overview and "polyfill · 0" on a placeholder page; with `originAgentCluster` forced to `false` by an init script (the runtime refuses every registration — what Firefox and WebKit did before ADR-0006 (5)), on every engine: `data-webmcp` is still `ready`, the indicator reads "Agent tools: unavailable", `data-webmcp-error` matches `get_balance: SecurityError` and the console holds a warning naming `get_balance` (v1.0.5) | US-38 AC1–AC3, US-39 AC1/AC3/AC4, US-41 |
| E2E (off) | app renders; `document.modelContext === undefined` on every page; indicator "Agent tools: unavailable" | US-38 AC2, US-41 |
| Headed runbook | Chrome ≥149 + flag: indicator "native · 2"; Tool Inspector lists both tools and can call them | NFR-B2 |

## 8. Out of scope
Relay/desktop clients (documented only), declarative form tools, third-party React/Angular hooks.

## 9. Open questions
None.
