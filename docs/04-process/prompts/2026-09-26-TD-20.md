# Prompt — TD-20: `pg` treats `sslmode=require` as `verify-full` today; `pg` 9 will not

The same background Claude Code session (Sonnet 5) as `2026-09-26-TD-19.md`, started from the
current `main` (`0d60891`) with the repository's `AGENTS.md`, `CLAUDE.md` and the owner's memory
loaded. The owner's instruction, verbatim (Azerbaijani; "Prepare TD-19 and TD-20"), which covers this
pull request and TD-19's:

    TD-19 və TD-20 hazırla.

It answered the agent's list of what was left after T-14 (`docs/03-specs/backlog.md` v1.43), where
TD-19 and TD-20 were the two entries the owner had kept for their own small pull requests at PR #60's
merge gate (2026-09-26, "2 ayrıca", "3 ayrıca"). The owner's decision on TD-20 is recorded in its
entry: not fixed in T-14; a separate small pull request; "the agent's recommendation was to rewrite
`sslmode` to `verify-full` in `createDb`, with a unit test on its options".

The brief the agent worked from is the entry itself — `docs/03-specs/tech-debt.md`, TD-20, its **Fix**
line and the options it lists as working and not working — plus the same standing rules as for TD-19:
`AGENTS.md` §2 (one change per PR, conventional commits, the process log),
`docs/03-specs/definition-of-done.md` (a fixture that shows what a guard prevents, tests that fail
first), `docs/04-process/governance.md` (the owner merges) and the owner's memory notes (a worktree
per task, `npm ci --ignore-scripts` and `npx prisma generate`, `npm audit` stays at 0).
