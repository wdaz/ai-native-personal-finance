# Prompt — T-08 App shell part 2: meta, reset banner, admin reset (Claude Code)

The session ran in Claude Code on the web (a cloud container), on the session branch
`claude/ecstatic-curie-4i0hw0`. It began with the owner's message, verbatim:

    Start planing T-08

Times below are the container's clock, UTC (the owner's local time is +04). The commit
timestamps give the order.

## Planning (plan v0.1–v0.4)

Under `docs/04-process/build-workflow.md` §2, a request for planning is a request for the plan
only. The agent read AGENTS.md, the roadmap, governance, the T-08 backlog row (v1.19),
SPEC-app-shell, SPEC-reset-and-test-support, ADR-0007, the tech-debt file, the Definition of
Done and the code T-05 and T-07 left behind. It wrote
`docs/04-process/plans/2026-09-23-T-08.md` v0.1 and stopped at the gate with two questions:
Q1, Vercel's cron sends `GET` but the spec says `POST` with a body, and Q2, the banner's
dismiss control has no design. A stop hook asked for the untracked plan to be committed; the
agent committed and pushed the plan only (`a9eccab`, 21:53).

The owner replied, verbatim, with `app-prototype.html` attached:

    2. Burda design var. Banner üçün referanse olaraq bura bax.

    1. Təkrar cəhd et. Alınmasa həmin saytları necə əlavə etmək lazımdır icazə olsun başa salarsan

("2. The design is here. Look here as the reference for the banner. 1. Try again. If it
doesn't work, explain how to add those sites so they are allowed.") The prototype is drawn on
a canvas, so the agent rendered it headlessly with Chromium and Playwright, logged in and read
the Overview page: there was no banner anywhere (plan v0.2, `60d52ea`). `vercel.com` was still
blocked. The agent explained where the environment's network access is edited. A Claude Design
link the owner sent while the turn was running (`claude.ai/design/p/db8d6b1f-…`, "Desing-a
həmçinin burdan baxa bilərsən. CLaude design") answered 403: it needs the owner's own login.

After the owner switched the session's model to Opus 5.5, they sent Claude Design's standard
hand-off text ("I exported this design from Claude Design. Read README.md in this workspace,
then the chats/ transcripts, and implement the designs as described there. Ask me before you
start if anything is unclear. Focus on these files: `Finance App (standalone).html`"). None of
those files were in the workspace, so the agent asked. It asked what scope was meant, since the
text taken literally would skip the open plan gate. It also recalled the licence rule: Claude
Design exports are never committed (`docs/00-discovery/inputs/design/README.md`).

The owner then wrote, verbatim:

    Use the claude_design MCP (https://api.anthropic.com/v1/design/mcp, auth via /design-login) to import this project:
    https://claude.ai/design/p/db8d6b1f-b7b6-4552-b71e-62cc0e966214?file=Finance+App+%28standalone%29.html

    Focus on these files (the whole project is readable):
    - `Finance App (standalone).html`

    Bunu yoxla. Mümkünsə Azərbaycan dilində mənə cavab ver

("Check this. If possible, answer me in Azerbaijani.") The design tool (`DesignSync`) needs
`/design-login`, which cannot run in a web session. The agent said so, in Azerbaijani, and
suggested "Send to Claude Code Web" or a zip upload.

The owner uploaded the export as a zip, with, verbatim:

    Vercel Cron nəyi GET yollayır?

("What does Vercel Cron send via GET?") The zip was unpacked into the session scratchpad and
not added to the repository. It had no banner and no "x" icon, but its modals have a designed
close control (`closeCircle`). That became Q2's new recommended option (d) (plan v0.3,
`148ea11`). The agent answered the cron question from its trained knowledge and said so.

    Vercel.com icazə əlavə etdim. Yenidən cəhv et

("I added permission for vercel.com. Try again.") `WebFetch` was still refused, but `curl`
through the proxy reached `vercel.com`. The three cron pages confirmed it: Vercel sends a
bodyless `GET`, `CRON_SECRET` goes in the `Authorization` header, and redirects are not
followed (plan v0.4, `55239c5`).

## The go-ahead and execution

~22:18 UTC, verbatim:

    tövsiyə olunanlarla başla

("Start with the recommended ones.") That means Q1 (a) and Q2 (d), plus the go-ahead. Before
writing code, the agent asked the advisor (a stronger reviewer model) to check the approach.
Its twelve corrections are recorded in the plan (v0.5, `ab6a698`). Among them: the CI API job
lacked the new secrets, the plan's unset-`CRON_SECRET` test could never run over HTTP, the
shared `toErrorIssues` throws on the admin body's issue codes, and D8's `setState` in an effect
would fail lint.

The environment had no Docker daemon and no Node 26. The agent installed Node 26.10.0 from
nodejs.org and ran a local Postgres 16.13 on port 5432. It wrote a git-ignored `.env.local`
with fresh random local secrets. For E2E it used a session-local Playwright config in the
scratchpad that points Chromium at the preinstalled r1194. The native tests executed in the
session itself: no subagents wrote code.

Commits: `229ebf0` (env accessors, threshold), `4dded1d` (meta, `latestReset`,
`x-request-id`), `05aad0c` (admin reset, `vercel.json`, CI secrets, SPEC v1.3), `5d606c0` (the
banner), then the READMEs and this record.
