# Prompt — T-01 Scaffold (Claude Code, first session of Phase 5)

Read AGENTS.md and follow its reading order. We are in Phase 5 (Build), Release 1.

Implement task **T-01** from docs/03-specs/backlog.md exactly as scoped there:
- Next.js App Router, TypeScript strict, per ADR-0001 and the folder layout of ADR-0002 (create the folders with README stubs where empty).
- ESLint with import-boundary rules from ADR-0002, Prettier, Vitest, Playwright configured for Chromium/Firefox/WebKit (no tests yet beyond a smoke test).
- `src/ui/tokens.css` generated from docs/02-architecture/design-tokens.md; Public Sans via next/font; copy the challenge avatars from docs/00-discovery/inputs (ask me for the assets folder if you cannot find them) into public/avatars/ using the basename keys described in SPEC-overview §4.5.
- `.env.example` listing every variable named in ADR-0005/0006/0007 and SPEC-webmcp-tools §2.1.
- README section "Run locally" (one command each for install, dev, test).
- Minimal GitHub Actions workflow: install, lint, typecheck, unit.

Before writing code, give me a short plan (files you will create, decisions you need from me) **and stop there — do not run any write tool until I reply**. After my go-ahead, implement, run lint/typecheck/unit, and finish with the Definition of Done checklist (docs/03-specs/definition-of-done.md) ticked or explicitly not applicable, plus a draft process-log entry using docs/templates/process-log-entry.md.
Do not implement anything from T-02 onward. Do not change docs/ except to add a changelog line to a spec if you find it wrong — tell me first.


*Note added 2026-09-20 after the session: the original wording ("Then implement") was read as authorisation to continue without the owner's reply; the stop instruction above is the corrected template for later tasks.*
