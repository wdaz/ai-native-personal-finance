# Prompt — TD-19: the proxy for Next's `.segments/*` and `.json` transport URLs

A background Claude Code session (Sonnet 5), started from the current `main` (`0d60891`) with the
repository's `AGENTS.md`, `CLAUDE.md` and the owner's memory loaded. The owner's two messages,
verbatim (Azerbaijani; the second is the instruction, the first is the question it answers):

    Növbəti hansı işlər qalib?

    TD-19 və TD-20 hazırla.

The first message ("What work is left next?") was answered from `docs/04-process/roadmap.md`,
`docs/03-specs/backlog.md` v1.43 and the headings of `docs/03-specs/tech-debt.md`: TD-19 and TD-20
are the two entries the owner had kept for their own small pull requests at PR #60's merge gate
(2026-09-26, "2 ayrıca", "3 ayrıca"). The second message is the request ("Prepare TD-19 and
TD-20"): one pull request each, prepared as drafts, not merged.

The brief the agent worked from is the entry itself — `docs/03-specs/tech-debt.md`, TD-19, its
**Fix** line — plus these standing rules: `AGENTS.md` §2 (one change per PR, conventional commits,
the process log), `docs/03-specs/definition-of-done.md` (a fixture for every new guard, tests that
fail first), `docs/04-process/governance.md` (the owner merges; code-review subagents use Opus 5.5),
and the owner's memory notes (a worktree per session, `npm ci --ignore-scripts` and `npx prisma
generate`, out-of-scope findings as separate PRs).
