# Prompt — T-11 WebMCP adapter (Claude Code)

The session ran in Claude Code on the web (a cloud container), on the session's designated
branch `claude/t-11-planlamasi-xmew0s`, already at `origin/main`'s tip (T-10's PR #22 merged as
`099b91b`). The owner's first message, verbatim:

    T-11 planlamasına başla

("Start planning T-11.") Under `docs/04-process/build-workflow.md` §2, this is a request for
the plan only.

## Planning

The agent read `AGENTS.md`'s reading order, the T-11 backlog row (v1.20), SPEC-webmcp-tools
(v1.0.2) in full, ADR-0004, the relevant NFRs (NFR-W1–W3, W9) and user stories (US-38, US-41),
`definition-of-done.md`, `docs/03-specs/tech-debt.md` (TD-5), and the code T-04/T-07/T-08 had
already left for this task: `src/shared/tool-schema.ts` (`toolInputJsonSchema`, T-04),
`src/shared/schemas.ts`'s `WebMcpModeSchema`, `next.config.ts`'s `NEXT_PUBLIC_WEBMCP_MODE`
exposure, `app/(app)/layout.tsx` and `src/ui/Shell.tsx`/`Sidebar.tsx`/`PageHeader.tsx` — each of
which already carried an explicit "T-11 adds ..." comment naming where the indicator and the
provider belong.

Two things drove most of the plan's own content, not the spec text alone:

1. **The eslint boundary rule that `src/ui` may not import `src/webmcp`** (already noted in
   `Shell.tsx`'s own comment) conflicts, on its face, with "the indicator renders inside
   `Sidebar`/`PageHeader`". The plan's Decision D1 resolves it: `AgentToolsStatus` stays in
   `src/webmcp`; `src/ui` gets a bare `ReactNode` slot with no WebMCP knowledge; `app/(app)/
   layout.tsx` — the one file ADR-0002 lets import both layers — is where the two meet.
2. **Reading the actually-installed `@mcp-b/webmcp-polyfill@5.1.0`'s own source** (its README's
   compatibility-boundary section, and `dist/index.js`/`dist/schema.js` read directly), rather
   than trusting SPEC-webmcp-tools §2.2's text (written 2026-09-13/09-20 against a 4 September
   2026 draft the project's own research note had already flagged as certain to move), found
   two real mismatches, written into the plan as Q1/Q2: the package exposes no
   `unregisterTool(name)` at all — tool lifetime is `AbortSignal`-based — and a registered
   tool's `execute` never receives a runtime signal, only the parsed input. The upstream spec
   page itself (`webmachinelearning.github.io/webmcp`) could not be re-checked — that host is
   blocked by this session's network egress policy — so the installed package, which the app
   will actually run against, was the primary source, not a secondary one.

The plan (`docs/04-process/plans/2026-09-24-T-11.md`) was committed and pushed on its own
(`8a66e31`), and the agent stopped and reported it — Q1/Q2 and the recommendation to accept
them "as recommended" — without touching any implementation file, per the plan gate.

The owner's second message, verbatim:

    start

## Execution

The agent worked through the plan's file list in order: `types.ts`/`adapter.ts` (mode
detection, the `AbortController`-per-generation `register`/`unregisterAll`, `onStatus`, the
`window.__pf` test hook); `defineTool.ts`; `WebMcpProvider.tsx`/`WebMcpTools.tsx`/
`status-context.ts`; `AgentToolsStatus.tsx` (+ CSS); the `src/ui` slot and its wiring into
`Sidebar`/`PageHeader`/`app/(app)/layout.tsx`; the doc updates (READMEs, the SPEC v1.0.3
changelog, TD-5's closure, the boundary fixture repoint); 38 new unit tests
(`tests/unit/webmcp/`).

Two real defects were found and fixed before declaring the task done, both described in full —
with the exact commands and output — in the process-log entry for this date, not repeated here:
a Server-Component-reads-`.Provider`-off-a-client-context crash (React error #130, every
authenticated page affected) that every one of the 38 unit tests missed and only a real
`next dev`/`next build` run through an actual login caught; and a `register()` bug that counted
a tool as registered even when the runtime rejected it, found and fixed while writing the
adapter's own tests. A third, smaller fix came from `AgentToolsStatus`'s own component test: the
ARIA `status` role does not name itself from content, so the sidebar variant needed an explicit
`aria-label` it had not had.

Local environment setup this session needed, not part of the deliverable: PostgreSQL 16 as a
native service (`service postgresql start` — no Docker daemon here, matching prior sessions'
own notes, though this is the first session to find the native Postgres already installed
rather than concluding no database was available), a `personal_finance` database, and
`.env.local` with a locally generated demo bcrypt hash and test secrets. `npx playwright test
--project=chromium` failed immediately on every test (a Chromium build mismatch between the
pinned `@playwright/test` version and what is pre-installed at `/opt/pw-browsers`); rather than
work around the harness the way T-10's session did (a local Playwright config), this task's own
verification needed only a handful of real page loads, so a small standalone script launched
Chromium directly at `/opt/pw-browsers/chromium`, logged in as the demo account, and checked the
indicator's text, `aria-label` and DOM placement in the expanded sidebar, the collapsed sidebar,
and at 500 px width, under both `next dev` and a clean `next build && next start` — this is what
caught the crash above, and confirmed the fix, and confirmed TD-6's CSP relaxation really is
dev-only (a clean production console). The script was deleted before every commit and is not
part of the diff.

No adversarial or advisor review pass ran before or during this session, unlike T-08's and
T-09's own sessions — noted as a process gap in the log entry, not hidden.

Before declaring done: `npx tsc --noEmit`, `npm run lint`, `npm run format:check`, `npm test`
(796 — 758 + this task's 38), `npm run test:api` (91, unchanged), `npm audit --audit-level=high`
(0), and `npm run build` (clean, Turbopack) all green.
