# Prompt — T-09 Overview server + API (Claude Code)

The session ran in Claude Code on the web (a cloud container), on the session branch
`claude/ecstatic-curie-4i0hw0`. It continued the same session that had just finished T-08 (PR
#20, merged as `3ed32ad`). The owner's message, verbatim:

    T-09 planlamağa başla

("Start planning T-09.") Under `docs/04-process/build-workflow.md` §2, a request for planning
is a request for the plan only.

## Planning

Before writing the plan, the agent found that GitHub had deleted the remote branch
`claude/ecstatic-curie-4i0hw0` once PR #20 merged (`git fetch` failed with "couldn't find
remote ref"), so the session's designated branch was restarted from the latest `origin/main`
(`git checkout -B claude/ecstatic-curie-4i0hw0 origin/main`), per this session's own git rules
for a merged branch.

The agent then read the T-09 backlog row (v1.20), SPEC-overview (v1.1) §1–§7, ADR-0002,
ADR-0005, data-model.md and the relevant NFRs, and the code T-02/T-03/T-04 had already left
behind for this task: `src/domain/overview.ts`'s `overviewSummary` (already fully unit-tested),
`src/shared/schemas.ts`'s `OverviewDtoSchema` (already built, strict), `src/server/seed.ts`'s
`CATEGORY_BY_NAME`/`THEME_BY_HEX` forward maps, and `scripts/seed-figures.ts`. All of the
harder design questions — which layer converts `BigInt` to `Number`, which layer maps Prisma's
enum spelling to data-model.md's, that `budgetSpent`'s categories must agree between budgets
and transactions — were already resolved by the T-02/T-03/T-04 plan gates' hand-offs written
into the T-09 backlog row. The agent found no question that needed the owner's judgment (no
design or product decision was open), so the plan skipped straight to recording its own
implementation decisions.

Before writing any code, the agent called the advisor (a stronger reviewer model) to check the
plan's first draft. It found five real defects — Task 2's steps out of TDD order; the two
enum-spelling label maps designed as hand-typed reverse tables that no planned test could have
caught a swapped entry in; a missing seed variant; two factually wrong statements (a
reset does invalidate an existing session; the mutation's worked-out number was wrong); and a
DoD-scope note the plan needed. All five were corrected in the plan before it was shown to the
owner (`docs/04-process/plans/2026-09-24-T-09.md`, "Pre-execution review (advisor)"), which
also adopted the advisor's pure/impure split suggestion (`toOverviewDto` / `getOverview`) and
replaced a planned dependency on `scripts/seed-figures.ts` (whose import shape — `import …
with { type: "json" }`, `import.meta.main` — no existing `tests/api` spec had proven safe in
Playwright's runner) with an independent oracle built directly from `seedRows()` and
`applyVariant()`.

The plan (v0.1) was committed and pushed alone (`cb38393`), and the agent stopped at the gate.

## The go-ahead

The owner replied, verbatim:

    Start implementing

## Execution

Commits: `40585b3` (`src/server/overview.ts` — the label maps, `toOverviewDto`, `getOverview`;
`tests/unit/server/overview.test.ts`), `711b2b9` (`app/api/overview/route.ts`;
`tests/api/overview.spec.ts`; the stale-comment fix in `tests/api/middleware.spec.ts`), then
the READMEs and this record.

Both tasks followed red/green: the unit test file was written and run to see it fail (the
module did not exist) before `overview.ts` was written; the API test file was written and run
to see the real, mixed failure the plan predicted (the 401 test already passed — the
middleware answers before Next resolves any route — while every authenticated test 404'd)
before the route was created.

Three of the plan's Review Focus mutations were run and reverted, each caught by the test that
should catch it:

- Removing `categoryLabel()` from the transactions side of the mapping (leaving budgets
  mapped, transactions raw) dropped the seed's `budgets.spent` from `$338.00` to exactly
  `$165.00` — matching the advisor's corrected prediction to the cent — and 4 of the 6
  seed-variant API tests failed on the exact-equality assertion.
- Spreading `...transaction` instead of naming each field in `toOverviewDto` surfaced the
  leaked `seq`/`category`/`recurring` in the pure unit test immediately, no database needed.
- Removing the route's `Cache-Control: no-store` header failed the exact-header API test.

Commit `23cb99e` closed out the READMEs, the process-log entry and this record — at which point
the agent called the advisor a second time, on the finished code rather than the plan. It found
the map-construction guard in `overview.ts` checked only one direction (every `CATEGORIES`/
`THEMES` label had a matching Prisma key; nothing checked the reverse, so a Prisma member with
no label would have built successfully and only failed at request time), a misquote of the
owner's trigger message in the process log, and that the plan's own status line claimed every
Review Focus mutation had run when only three had. `overview.ts`'s two maps are now built by one
exported `labelMap(labels, prismaEnum)` that also checks the two sides are the same size and
that no two labels collide on one key; `tests/unit/server/overview.test.ts` gained five fixtures
for it, including the classic `"constructor" in {}` trap a bare `in` check (rather than
`Object.hasOwn`) would have fallen for. Commit `0743ca7` had this fix and the wording
corrections, and was pushed.

A third advisor call, on that pushed commit, found the fix's own commit message and the plan's
"Execution corrections" both claimed a run that had not happened ("built `CATEGORY_LABEL`
without complaint under the one-directional version") — reasoned from reading the old code, not
executed. The agent then actually ran it both ways: `git show 40585b3:src/server/overview.ts`
restored to disk, `"Dining Out"` commented out of `CATEGORIES`, and a `tsx` script printing
`CATEGORY_LABEL.size` — the one-directional file printed `9` with no throw; the fixed file, same
mutation, threw `"9 labels but 10 Prisma keys — the two enums have drifted"`. Both files were
restored (`git diff --stat` empty) and the plan/process log updated with the real output instead
of the reasoned claim. The same call also found two redundant, fixture-less assertions in
`overview.test.ts` that re-checked what `tests/unit/shared/enums.test.ts` already holds to
data-model.md, with its own violation fixture — removed, with a comment pointing to that file
instead of duplicating it.

Environment: Node 26.10.0 and the local Postgres 16 process (`pg_ctl`, not a container — this
environment has no Docker daemon) from the T-08 session were already running and were reused as
is. This task adds no E2E test of its own, but the existing suite was run anyway through T-08's
session-local Playwright config (Chromium r1194): 76/76 passed. `npm test` (699 unit), `npm run
test:api` (91), `npm run lint`, `npm run format:check`, `npm run typecheck`, `npm audit` (0) and
the full-history secret scan all ran and passed, both before and after the correction.
