# Prompt — T-07 App shell part 1 (Claude Code)

The session began with the owner's message, 2026-09-23 ~18:09 +04, verbatim:

    Start to planing T-07. main branch updated

The session ran in the background, right after T-06's merge. Under
`docs/04-process/build-workflow.md` §2 a request for planning is a request for the plan only, so
the agent read the T-07 backlog row (v1.18), SPEC-app-shell, SPEC-auth §2.7–2.9, the Definition of
Done, US-03, US-32–US-35 and the copy appendix, ADR-0002/0003/0006, the design tokens, the
codebase and the design export outside the repository (`~/Own/design-exports/app-prototype.html`,
`style-guide.html`). It then wrote `docs/04-process/plans/2026-09-23-T-07.md` (v0.1) with the
superpowers `writing-plans` skill and stopped at the plan gate: four questions (Q1–Q4) — the
logout that cannot reach the login page when its request fails, `/overview` before T-10, the
"Log out" icon, and nine design values that are not tokens yet.

## Owner's replies at the plan gate

~18:52 +04, verbatim:

    Q1–Q4 tövsiyyə olanları plana daxil et. gate PR yolunun təsdiqi - bunu anlamadım?

("Put the recommended answers to Q1–Q4 into the plan. The approval of the gate PR path — I did
not understand this.") The agent applied the recommended answers (plan v0.2) and explained the
gate-PR question: the specs and the ADR that T-07 changes must be on `main` before the code
starts, because the code follows them.

~18:54 +04, verbatim:

    bəli, hazırla

("Yes, prepare it.") The amendments (SPEC-auth v1.0.6, ADR-0006, SPEC-app-shell v1.3,
design-tokens v1.2, backlog v1.19, tech-debt v1.2) and the plan (v0.3) went into one docs-only PR,
the gate PR (#18), which the owner merged.

## Go-ahead

~19:06 +04, verbatim:

    /superpowers:subagent-driven-development istifadə edərək T-07 icrasına başla

("Start T-07's execution using [subagent-driven-development].") The plan had recommended native
execution; the owner chose subagent-driven at the go-ahead. The controller created
`task/T-07-app-shell` from `origin/main` and ran the plan's seven tasks with one Sonnet
implementer and one Sonnet reviewer per task (a fix round where a reviewer confirmed a finding).
Task 7's documentation, the full gate and the screenshots were the seventh implementer's; the
whole-branch review, the push and the draft PR belong to the controller.

## Screenshots

`2026-09-23-T-07/screenshots/` — `overview` and `transactions` at 1440, 768 and 375 px, and
`transactions-1440-minimised.png`, taken from a production build (`APP_ENV=test`) by a script
that read the demo credentials from the environment. See the process-log entry for what was
compared with the design export.
