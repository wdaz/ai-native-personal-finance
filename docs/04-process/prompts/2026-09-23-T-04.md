# Prompt — T-04 Shared schemas, enums, copy and test ids (Claude Code)

The planning session began with the owner's message, 2026-09-23 09:26 +04, verbatim:

    T-04 planı hazırla

("Prepare the T-04 plan.") It arrived in a background session already inside the repository,
right after T-03's PR #8 had merged. Under `docs/04-process/build-workflow.md` §2 a request for
a plan is a request for the plan only, so the plan was the only file the planning session wrote.

The agent read the current state of `docs/03-specs/backlog.md`'s T-04 row (v1.8, narrowed by the
T-03 plan gate to the schemas, enums, copy and test ids), the specs T-04 depends on (SPEC-auth,
SPEC-overview, SPEC-app-shell, SPEC-webmcp-tools, the copy appendix, ADR-0002/0003/0004,
data-model.md), then measured a full prototype in a scratch copy of `main` at `90440a1`: Zod 4
probes (`z.toJSONSchema`, `z.iso.datetime`, `z.int`, issue shapes), every file of the six planned
tasks built and gated (Prettier, ESLint, tsc, Vitest: 341 → 415), 26 mutations against the new
code (all killed after two tests were rewritten to stop reading the schema's own constants), and
the whole plan replayed task-by-task on a second clean copy — red, then green, at every step. It
wrote `docs/04-process/plans/2026-09-23-T-04.md` (v0.1, commit `a246ded`) with the superpowers
`writing-plans` skill, and stopped at the plan gate with six questions and three findings outside
the task (F1: `zod-to-json-schema` returns an empty JSON Schema for a Zod 4 object with no error,
contradicting ADR-0004 and SPEC-webmcp-tools §2.4; F2: SPEC-auth §6's error bodies were missing
the `message` field §2.10 requires; F3: a Vitest deprecation notice, for T-13).

## Owner's replies at the plan gate

2026-09-23 09:29 +04. The whole message was pasted text, verbatim:

> Cavablar:
>
> 1. Kateqoriya/tema adları — qəbul. API-də sənəd adları ("Dining Out"). Prisma enum ↔ sənəd adı uyğunlaşdırması T-09-da src/server-də, tək bir map ilə. Şərt: src/shared/schemas.ts-dəki enum siyahısı sənədlərdəki adların tam siyahısı olsun ki, T-09 map-i test edə bilsin.
>
> 2. Qeydiyyat limit mesajları — qəbul. Ad > 60 və şifrə > 128 üçün appendix-ə iki yeni sətir; email > 254 üçün mövcud "Enter a valid email address". Yeni mesajlar mövcud üslubda olsun (qısa, nöqtəsiz). user-stories v1.2 bunu əhatə edir.
>
> 3. Rəqəmli mətnlər — hər ikisi qəbul. (a) 1 minute / N minutes — auth.md §2.4-dəki düstur (N = max(1, ceil(retryAfter/60))) qalır, yalnız cəm/tək dəyişir; unit testdə retryAfter=30 → "1 minute", =90 → "2 minutes". (b) Banner {days} parametri ilə; dəyər GET /api/meta-dan gəlsin, kodda 10 hardcode olmasın. app-shell v1.1-də §2.6 buna uyğun düzəlsin.
>
> 4. test-ids.ts — qəbul. Boş reyestr + string data-testid yazılmasını qadağan edən ESLint qaydası (JSX attribute literal). Qayda üçün bir keçən, bir rədd edilən fixture olsun.
>
> 5. http.ts — qəbul. ApiErrorCode-u ErrorEnvelope schema-dan çıxarılan tipə bağla (z.infer), errorResponse imzası və cavab məzmunu dəyişməsin. Mövcud testlər olduğu kimi keçməlidir.
>
> 6. Sənədlər — qəbul. user-stories v1.2, app-shell v1.1, backlog v1.9 bu PR-da. F2-yə görə auth.md-də də kiçik versiya artımı olacaqsa (aşağıda), o da bura.
>
> Task-dan kənar tapıntılar:
>
> F1 — razıyam, T-04-dən əvvəl ayrıca sənəd PR-ı. ADR-0004 və webmcp-tools §2.4-də zod-to-json-schema → Zod 4-ün z.toJSONSchema; $refStrategy qeydi də uyğunlaşdırılsın. Əlavə şərt: T-04-də defineTool üçün bir test — Zod schema-dan alınan JSON Schema-da maxLength həqiqətən görünür; boş schema gələrsə build-də throw. Bu, eyni problemin gələcəkdə səssiz qayıtmasının qarşısını alır.
> F2 — bu PR-da düzəlsin (auth.md-də kiçik dəyişiklik): §6-da 401 → message: "Email or password is incorrect", 429 → message: "Too many attempts" + retryAfter. §2.10 qaydası dəyişmir.
> F3 — qəbul, T-13 sətrinə qeyd kimi.

Because the reply arrived as pasted text with no words of the owner's own around it, and because
two of its points needed a decision the plan's questions had not asked (the exact shape of the
test-id rule; where F1's runtime check should live), the agent asked three clarifying questions
through the structured question tool rather than guess. The owner chose, verbatim option labels:

- Scope of the reply: **"Yalnız plan v0.2 (Recommended)"** — apply the answers to the plan,
  commit and push it; the F1 PR and the plan's execution wait for a separate go-ahead.
- The test-id rule's exact shape: **"JSX + getByTestId (Recommended)"** — two ESLint selectors (a
  JSX `data-testid` string literal, and a `getByTestId(...)` string-literal argument), one
  rejected and one passing fixture per side.
- Where F1's guard lives: **"T-04: shared helper (Recommended)"** — a function in `src/shared`
  (`toolInputJsonSchema`) that T-11's `defineTool` calls later, rather than a bare test with no
  reusable code, or deferring the whole check to T-11.

The agent applied every answer to the plan, added a new Task 5 (`toolInputJsonSchema`) that did
not exist in v0.1, re-measured the prototype (32 mutations, 427/427, tasks replayed in order
again: 348 → 364 → 395 → 412 → 420 → 427 → 427), and wrote plan v0.2 (`docs/04-process/plans/2026-09-23-T-04.md`,
commit `cf84f4b`), reporting the changes and the six answers' table back to the owner.

## The go-ahead

2026-09-23 09:40 +04, verbatim:

    F1 ayrı sub agent etsin.
    Başla.

("Have F1 done by a separate subagent. Start.") The agent read this as the separate go-ahead the
plan-gate answer had reserved, and as an instruction to run F1's docs PR (ADR-0004,
SPEC-webmcp-tools §2.4) through its own subagent, in parallel with this plan's execution rather
than in this session directly. Both began at 09:41 +04.

## Execution

F1 was dispatched to a fresh subagent in its own worktree (branch `worktree-agent-a86c7e98b28fa2e37`
from `origin/main`), which amended ADR-0004 (a new "Clarification 2026-09-23" line; the ADR stays
Accepted) and `docs/03-specs/webmcp-tools.md` (v1.0.2) to name `z.toJSONSchema` instead of
`zod-to-json-schema`, opened draft PR #9, and reported back; the controller verified the diff
independently against the brief before trusting it. The owner merged PR #9 at 2026-09-23T05:48:01Z
(merge commit `1a0a2e6`).

T-04 itself ran under `superpowers:subagent-driven-development` on branch `task/T-04-shared`, one
fresh implementer subagent per task and one fresh reviewer per task (mostly `haiku` implementers
against a fully-specified brief, `sonnet` reviewers), plus one fix round on Task 5: a real
nullable/union-typed-string blind spot in the new `maxLength` guard, and a `z.uuid()` exemption
the task review mistakenly believed the plan's decision D17 called for — D17's own table lists
that exemption in its "Alternative rejected" column, not its "Decision" column. Both were
"fixed" and re-reviewed clean at the time; the final whole-branch review (`opus`) caught the
misreading against the plan's own table and against SPEC-webmcp-tools §2.4, ADR-0004 and this
branch's own T-15 hand-off, and a further fix wave reverted the uuid exemption while leaving the
nullable/union fix, which the final review independently confirmed correct, standing. The full
ledger — the pre-flight conflict scan, every task's dispatch, report and review verdict, the fix
round, the final review and its fix wave, and every ruling made along the way — is at
`.superpowers/sdd/2026-09-23-T-04/progress.md`, and is summarised in the T-04 entry of
`../process-log.md`.
