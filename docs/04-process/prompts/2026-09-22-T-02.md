# Prompt — T-02 Persistence, seed and test support (Claude Code)

The planning session began with the owner's message, verbatim:

    Read AGENTS.md and docs/04-process/build-workflow.md, then take task T-02 from
    docs/03-specs/backlog.md — plan first with writing-plans, stop and wait for my go before
    any write.

    Create file with this name docs/04-process/prompts/2026-09-22-T-02.md and save.

(Line breaks added for width; the words are the owner's.) The second paragraph is why this
file exists before the plan gate: the owner asked for the prompt to be saved under this name,
so it and the plan were the only two files the planning session wrote before the owner's
reply (the two small PRs below came after it, on their own branches).

The session was interrupted once, before any file had been written. The owner's message,
verbatim:

> Son dəyişikliyi pull etməmişdim deyə dayandırıb pull etdim. Davam edə bilərsən.

("I had not pulled the latest change, so I stopped and pulled. You can continue.") The pull
brought `main` to `f38f7de` (PR #3 — ESLint and Prettier ignore the agent workspaces); every
measurement in the plan was taken on that commit.

The agent read AGENTS.md and its reading order, measured the premises of the backlog row in a
scratch copy of the repository (plan § "Evidence"), had one research subagent without write
tools collect the Prisma 7, Next.js 16, Postgres and Neon facts from their primary sources,
wrote `docs/04-process/plans/2026-09-22-T-02.md` with the superpowers `writing-plans` skill,
and stopped at the plan gate (build-workflow.md §2).

## Owner's replies at the plan gate

2026-09-22 17:08 +04. The whole message was pasted text, verbatim:

> Go with all recommendations, with these adjustments:
> 5 — move GET /api/test/log to T-12 in the backlog, but leave a one-line note in SPEC-reset §2.7 that it lands in T-12.
> 9 — use the overrides (audit must stay 0 behind the T-02a gate); comment the reason in package.json; add a T-13 note to drop the overrides when Prisma updates.
> 2 — add one sentence to the spec: BigInt columns are converted to Number at the DTO boundary (our limit is far below MAX_SAFE_INTEGER).
> Before T-02: two separate small PRs — (a) next.config agentRules: false with a process-log line that the tool tried to edit AGENTS.md; (b) fix(secret-guard): key the gitleaks cache by platform (os_arch), with its failing fixture. Document F2 (hooksPath is absolute; worktrees use the main checkout's hook) as a known limitation in the T-02a docs.

Because it arrived as pasted text with no words of the owner's own, the agent asked (in
Azerbaijani) whether it was the owner's plan-gate reply and how much of it to act on. The
owner chose, verbatim: "Bəli — plan v0.2 + PR (a), (b)" ("Yes — plan v0.2 and PRs (a) and
(b)"), whose stated meaning was: update the plan, record the reply here, open the two small PRs
as drafts for the owner to merge, and start no T-02 execution before a separate go-ahead.

17:42 +04, while the second PR was being opened, verbatim:

> davam et

("continue"). The agent opened
[#4](https://github.com/wdaz/ai-native-personal-finance/pull/4) (`agentRules: false`) and
[#5](https://github.com/wdaz/ai-native-personal-finance/pull/5) (the gitleaks cache keyed by
platform; F2 as a known limitation) and wrote plan v0.2. How each answer changed the plan is
in the plan's § "Owner answers at the plan gate".
