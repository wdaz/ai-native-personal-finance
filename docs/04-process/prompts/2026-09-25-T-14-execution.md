# Prompt — T-14 execution (2026-09-25 → 2026-09-26)

Trigger message (the owner, in Azerbaijani, 2026-09-25 22:55 +04): "başla. #59 merge oldu" ("Start.
#59 is merged"). It followed the plan gate (PR #59, `plans/2026-09-25-T-14.md` v0.2: Q1–Q13
answered "as recommended", project name `personal-finance`, Native execution).

The session took it as:

> Execute `docs/04-process/plans/2026-09-25-T-14.md` Tasks 1–7 on `task/T-14-deploy`, cut from
> `origin/main` (`47a5625`), the way `superpowers:executing-plans` runs inline: one ledger, TDD for
> code, every step's command run and its output read, every deviation a written ruling; stop at the
> owner's steps (Task 5) and at anything the harness refuses; one Opus 5.5 whole-branch review before
> the merge. Task 8 (production) waits for the owner's merge. Never merge, never push to `main`.

Read first, per `AGENTS.md` §1: the same documents as the planning session, plus the plan itself and
`docs/04-process/governance.md` (v1.4: review subagents run without write tools and use Opus 5.5).

## The owner's messages during the execution (verbatim, with a translation where not English)

- "3. bəli. 2. vercel --prod alınır? ! sh set-env.sh" — "3. yes. 2. can `vercel --prod` be used? [4:]
  `! sh set-env.sh`": approval of the demo password; the first production deployment through the
  CLI; the environment variables set by a script the owner runs.
- "No Results Found — Your search for "personal-finance" did not return any results." (pasted from
  Vercel's import screen), then the project's dashboard URL, then "No Production Deployment — Your
  Production Domain is not serving traffic." and "Your domain is properly configured, but you don't
  have a production deployment. To deploy to production, push to main, or run vercel --prod …" —
  the owner reporting where the import flow stood.
- The owner's own `!` commands, run in the session: `vercel env rm` for the 13 pre-filled variables;
  `vercel deploy <clean export of main> --prod --project personal-finance --yes`;
  `sh set-env.sh production` and `preview`; and `vercel alias set …`, which the harness rejected twice
  (any command line containing "alias") and which succeeded in a terminal outside the session.
- "Bu fayllar komputerdədir?" ("Are these files on the computer?"), "Hazır" ("Ready"),
  "yoxlaya bilərsən?" ("can you check?"), "https://personal-finance-cyan-kappa.vercel.app/ niyə bu
  açmır?" ("why does this not open?"), "pr 60 merge edim?" ("shall I merge PR 60?").

## What the agent did with them

- Every owner step in Task 5 but one was verified afterwards by the agent from the platform's own
  read APIs, without seeing a secret value: `vercel env ls`, `vercel api /v9/projects/…`, `vercel ls`,
  `neon branches list`, `gh variable list`. The exception is the Neon Console setting "Automatically
  delete obsolete Neon branches", which no read API the agent had exposes.
- Three agent commands were refused by Claude Code's auto-mode classifier: `vercel env rm` and a
  `shasum` of `.env.local` ("Secret-Store Writes"), and `vercel deploy --prod` ("Production Deploy").
  The agent did not route around any of them and handed the first and the third to the owner as `!`
  commands and a script. Separately, the harness rejected four command lines containing the word
  "alias" (two of the agent's, two of the owner's `!` runs of `vercel alias set`).
- The measurement scripts are kept in `prompts/2026-09-25-T-14/scripts/`. They read the 0600 files
  under `~/.config/personal-finance-deploy/` by sourcing them, pass secrets to `curl` through
  configuration files on stdin or in a 0600 scratch directory (never on a command line), remove those
  files on exit, and print statuses, header presence and booleans only. Their absolute paths point to
  the session's temporary directory: they are records of the method, not tools to run elsewhere.

The advisor (a stronger reviewer model that sees the whole transcript) was called three times in this
execution: before Task 1, before Task 5, and after Task 6, before the records. Its findings that
changed the work are in the process-log entry.

See `docs/04-process/process-log.md`, "Phase 5: T-14 — the deploy, up to the merge", for what was
produced and what the plan had predicted wrongly.
