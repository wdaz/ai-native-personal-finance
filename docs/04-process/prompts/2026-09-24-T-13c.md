# Prompt — T-13c tech debt TD-7–TD-11 (Claude Code)

The planning session was started by the slash command alone, no prompt text:

    /superpowers:writing-plans t-13c td-11.

The agent read `AGENTS.md` and its reading order, measured the premises of the backlog row (TD-7's
missing status push, TD-8's comment against the test, whether the tree already breaks TD-9's rule,
what TD-10's guard may and may not rely on, what TD-11's build downloads and what its licence is),
wrote `docs/04-process/plans/2026-09-24-T-13c.md` and stopped at the plan gate
(`build-workflow.md` §2). The plan is dated with the day the session started, 2026-09-24; the
owner's answers and the plan's second version are 2026-09-25. The plan is on branch
`docs/T-13c-plan` until the owner merges the plan branch (no PR opened yet); it is not on the task
branch.

## The execution brief

The plan was executed by a controller agent through the superpowers `subagent-driven-development`
skill, in one session on 2026-09-25, on branch `task/T-13c-tech-debt` (Tasks 1–6, cut from
`origin/main` `910ad2d`). The controller wrote no execution prompt of its own: each implementer
subagent was dispatched with the plan's text for its task, extracted into a *brief*
(`task-N-brief.md`). The briefs in `2026-09-24-T-13c/` are those extracts, so they are the prompts;
a brief holds the task's steps (files, interfaces, code, commands, and the plan's "Prediction" and
"Measured" lines). Each dispatch also carried a message from the controller — the context, the
rulings made so far, the report format. Those messages were not saved as files; the rulings that
changed what was built are in the process-log entry.

- **Roles.** Implementers Sonnet 5, one task at a time. Task 4's review and its re-review ran on
  Opus; the reviews of Tasks 1, 2, 3 and 5 and Task 5's re-review ran on Sonnet, although
  `governance.md` v1.3 says code review subagents use Opus 5.5 — the controller's first ruling was
  written before it had read that rule, and it found the deviation on 2026-09-25 while preparing
  Task 6. The whole-branch review runs on Opus 5.5 and covers Tasks 1, 2, 3 and 5 in full. A
  review that found something Important (Tasks 4 and 5) was answered by one fix round for the same
  implementer, in a new commit, and a scoped re-review followed Task 4's round.
- **Rulings the controller made on the owner's behalf** — where the plan was silent or wrong — are
  in the controller's ledger, which is not copied (see the README of the folder). The ones that
  changed what was built: Task 4's guard was hardened after the review although that changes the
  plan's Step 3 code (the plan's own "fails closed" requires it); Task 5's README wording was
  corrected; the expected unit total was carried forward as 1 043 (the plan says 1 035). The
  process-log entry lists them.
- **Task 6** (this records task) was dispatched with rulings that override its brief where the run
  differed: the measured numbers (1 043 unit tests, the pixel counts) instead of the plan's; the
  fix round of Task 4 recorded in the tech-debt entry and the process log; the six deviations of
  the run recorded in the log; two extra sentences in the layer READMEs; the record and its folder
  named with the plan's date, 2026-09-24, as T-13's are, while the versions and changelog dates
  inside the documents say 2026-09-25.

## Owner's answers at the plan gate

Q1–Q7, 2026-09-25, in the owner's words: "Q1 - a, Q2 - tövsiyə olan, q3 - tövsiyyə olan, q4 -
linter, q5 - tövsiyyə olan, q6 - tövsiyyə olan, q7 - t-13 a/b plana daxildir ki, tövsiyyə
edirsən?" ("tövsiyə olan" — the recommended one). As the plan records them:

| Question | Answer                                                                                              |
| -------- | --------------------------------------------------------------------------------------------------- |
| Q1       | (a) TD-8: fix the comment; the failing-first line is waived for it                                  |
| Q2       | (a) TD-10: the first seed of Neon is `POST /api/admin/reset`; SPEC-reset-and-test-support v1.6      |
| Q3       | (a) TD-10: any `VERCEL` or `VERCEL_ENV` refuses `APP_ENV=test`; the Playwright check in its config |
| Q4       | a linter for TD-9, not the recommended unit test: Stylelint 17.15.0, a new dev dependency           |
| Q5       | (A) TD-11: `@fontsource/public-sans` 5.3.0, static latin 400 and 700                                |
| Q6       | TD-11: the ESLint restriction and the provenance test                                               |
| Q7       | a question back — T-13a/b are not part of this plan; run T-13c on its own branch now                |

The execution method and the go-ahead — "start, subagent-driven" — came as the additional text of a
`/compact` instruction on 2026-09-25 and were then repeated as a plain message. The briefs and
reports are in `2026-09-24-T-13c/`.
