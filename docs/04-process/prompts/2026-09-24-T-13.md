# Prompt — T-13 CI hardening (Claude Code)

The planning session was started by the slash command alone, no prompt text:

    /superpowers:writing-plans t-13

The agent read `AGENTS.md` and its reading order, measured the premises of the backlog row
(Firefox and WebKit, the Prisma overrides, `strict-allow-scripts`, a drift check, the
commit-message scan), wrote `docs/04-process/plans/2026-09-24-T-13.md` and stopped at the plan
gate (`build-workflow.md` §2). The plan is on branch `worktree-t-13-plan` until its own docs PR
merges; it is not on the task branch.

## The execution brief

The plan was executed by a controller agent through the superpowers `subagent-driven-development`
skill, in one session, on branch `task/T-13-ci-hardening` (Tasks 1–11) and, for the defect fix that
had to come first, `fix/origin-agent-cluster` (PR-A, Tasks A1–A3, PR #28). The controller wrote no
execution prompt of its own: each implementer subagent was dispatched with the plan's text for its
task, extracted into a *brief* (`task-N-brief.md`; `pr-a-brief.md` for PR-A, which also carries
the plan's Global Constraints and Review Focus). The briefs in `2026-09-24-T-13/` are those
extracts, so they are the prompts; a brief holds the task's steps (files, interfaces, code,
commands, and the plan's "Prediction" and "Measured" lines).

- **Roles.** Implementers Sonnet 5; every task review and the whole-branch review Opus 5.5
  (`governance.md` v1.3). A review that found something was answered by a follow-up round for the
  same implementer; only Task 3's fix rounds had a scoped re-review, and the other follow-ups
  are covered by the whole-branch review.
- **Rulings the controller made on the owner's behalf** — where the plan was silent or wrong —
  were passed to the implementer with its dispatch or its fix round. They are in the controller's
  ledger, which is not copied (see the README of the folder); the process-log entry lists the ones
  that changed what was built.
- **Task 11** (this records task) was dispatched with rulings that override its brief: a new
  execution entry in the process log instead of completing the planning entry (the plan file and
  the planning entry are on the plan branch, not on main); no edit to the plan file; the overrides
  note names T-16 as owner.

## Owner's answers at the plan gate

Q1–Q8, as the controller relayed them to the implementers (only Q2 was recorded verbatim, in the
PR-A process-log entry):

| Question | Answer |
|----------|--------|
| Q1 | PR-A (the Origin-Agent-Cluster fix) is its own PR, merged before Task 10 |
| Q2 | a failed WebMCP registration must be reported even though `data-webmcp` stays `ready` |
| Q3 | four E2E legs: Chromium polyfill, Chromium off, Firefox polyfill, WebKit polyfill |
| Q4 | B — the axe gate scans every route and fails when a page is missing from the list |
| Q5 | yes — a commit and tag-message pass in the history scan |
| Q6 | yes — the traceability script reads a per-release story list; ADR-0003 wording proposed |
| Q7 | keep the Prisma overrides; the removal note goes to T-16 |
| Q8 | deny `fsevents` in `allowScripts` |

The controller's ledger records that Q4, Q5, Q7 and Q8 were answered "recommended" and that the
owner then said "start"; those four were not individually confirmed. The owner also asked, during
the planning session, for the worktree-bootstrap lesson to be recorded (`npm ci --ignore-scripts`,
then `npx prisma generate`; a symlinked `node_modules` does not work).

The execution method — subagent-driven development, with Opus 5.5 reviewers — was the owner's
choice, as the controller relayed it. The briefs and reports are in `2026-09-24-T-13/`.
