# Prompt — T-06 Auth UI (Claude Code)

The session began with the owner's message, 2026-09-23 ~14:53 +04, verbatim:

    T-06 start to prepare plan

The session ran in the background, right after T-05's PR #11 merged. Under
`docs/04-process/build-workflow.md` §2 a request for planning is a request for the plan only, so
the agent read the T-06 backlog row (v1.15), SPEC-auth (v1.0.3), SPEC-app-shell, the Definition
of Done, US-01/02/03/31/32 and the copy appendix, ADR-0002/0003/0006, the design tokens, the
codebase (shared schemas, copy, test ids, middleware, Playwright config, CI) and the design export
outside the repository (`~/Own/design-exports/app-prototype.html`, Auth screen). It then wrote
`docs/04-process/plans/2026-09-23-T-06.md` (v0.1) with the superpowers `writing-plans` skill and
stopped at the plan gate: four questions (Q1–Q4), one finding outside the task (F1, the
prerendered 404 page broken under T-05's CSP), and a question on how gate amendments reach `main`.

## Owner's replies at the plan gate

~15:29 +04, verbatim:

    /Users/ruslan/Own/design-exports dizayn faylları burda saxlanılıb.
    1 və 2 sualla bağlı bura bax. Cavab tapmasan yenidən o suallara qayıdarıq.
    3 və 4 üçün recomendention nədirsə onu et.
    B ilə davam edə bilərsən

("The design files are kept here. For questions 1 and 2, look there; if you don't find the
answer, we'll come back to them. For 3 and 4, do whatever the recommendation is. You can go
ahead with B" — B: the amendments as a docs-only PR.) The design export answered Q1 (a)/(b) and
Q2; Q1 (c) went back to the owner. Plan v0.2 and the amendments became PR #13.

~15:31 +04, verbatim:

    F1 ilə bağlı qərar T06 əvvəl ayrı subagent yarat o hazırlasın

("On F1: before T-06, create a separate subagent to prepare it.") The subagent's brief is
`2026-09-23-F1-not-found-csp.md`; its work became PR #15.

~15:41 +04, on Q1 (c), verbatim:

    2. Düzgün error mesaji çıxmalıdır. Hazırkı tövsiyyə olunan düz variant deyil.

("2. A correct error message must appear. The currently recommended option is not the right
one.") Asked for the wording (a structured question), the owner answered: "500 xətası üçün
Something went wrong. Try again olmalıdır. Səhv creadential və s hallarda uyğun mesaj" ("For a 500
error it must be 'Something went wrong. Try again'. For wrong credentials and other cases, the
appropriate message") and, for login, "Olduğu kimi qalsın (Recommended)" (leave it as it is). Then,
~15:44 +04:

    "Something went wrong. Try again" bu mesaj server xətası zamanı verilir

("This message is given on a server error.") Plan v0.3 → PR #14.

~16:19 +04, after F1's subagent found F2 (Next reads the nonce from the request's CSP header,
not `x-nonce`), verbatim:

    F2 tapıntısı techdeptdə yazılır. Və backlogda saxlanılır.
    PR-15 reviewsuna bax/

("F2 is recorded as tech debt and kept in the backlog. Look at PR #15's review.")

~16:23 +04, verbatim:

    Tech dept ayrıca fayl olsun. Backlogda bunun haqda qeyd salanılsın. Əlaqə itməsin deyə. PR-14 bundan öncə merge olub. Hara qeyd etmisən?

("Tech debt should be its own file, with a note in the backlog, so the link is not lost. PR #14
had already merged. Where did you record it?") The agent had pushed the tech-debt commits to PR
#14's branch after it merged; it said so, and carried them to `main` through PR #16
(`docs/03-specs/tech-debt.md`).

~16:30 +04: "PR - 15 merged. PR-16 has review" — the agent handled PR #16's three Copilot
findings.

## Go-ahead

~16:34 +04, verbatim:

    PR-lar merge oldu. bu suallarına təsdiq kimi qəbul olunur. Local main artıq hər iki pr-ı özündə əks etdirir. Lazımı dəyişiklikdən sonra T-06 başlaya bilərsən.

("The PRs are merged; take that as the answer to your questions. Local main already has both
PRs. After the necessary changes, you can start T-06.") The agent recorded it in plan v0.4,
created `task/T-06-auth-ui` from `origin/main` (`38e931d`), and executed the plan's eight tasks
inline with the superpowers `executing-plans` skill (the plan's recommendation), under
`test-driven-development`, with one fresh reviewer for the whole branch at the end.
