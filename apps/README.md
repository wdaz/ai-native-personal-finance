# apps/

Empty by design. Application code arrives in Phase 5, after the specs in
`docs/03-specs/` are approved. The layout (`apps/web`, `apps/api`,
`packages/shared`, or whatever ADR-0002 decides) is an architecture decision
and is not made here.
