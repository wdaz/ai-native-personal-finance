{
  "name": "ai-native-personal-finance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=26",
    "npm": ">=11.19"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "prepare": "sh scripts/install-git-hooks.sh",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0 && npm run lint:css",
    "lint:css": "stylelint \"app/**/*.css\" \"src/**/*.css\" --max-warnings 0",
    "lint:fix": "eslint . --fix --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:api": "playwright test --project=api --workers=1",
    "test:e2e": "playwright test --project=chromium --project=firefox --project=webkit",
    "test:e2e:ui": "playwright test --ui",
    "secrets:scan": "scripts/secret-scan.sh history",
    "test:all": "npm run secrets:scan && npm run lint && npm run format:check && npm run typecheck && npm run test:coverage && npm run traceability && npm run test:api && npm run test:e2e",
    "postinstall": "prisma generate",
    "db:reset": "prisma migrate deploy && prisma db seed",
    "db:drift": "sh scripts/schema-drift.sh",
    "seed:figures": "tsx scripts/seed-figures.ts",
    "traceability": "tsx scripts/traceability.ts"
  },
  "dependencies": {
    "@mcp-b/webmcp-polyfill": "^5.1.0",
    "@prisma/adapter-pg": "7.10.0",
    "@prisma/client": "7.10.0",
    "bcryptjs": "^3.0.3",
    "iron-session": "^9.0.1",
    "next": "^16.3.5",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0",
    "@testing-library/dom": "^10.4.2",
    "@testing-library/react": "^16.3.3",
    "@types/node": "^26.6.2",
    "@types/pg": "^8.23.1",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitest/coverage-v8": "^5.0.1",
    "eslint": "^9.39.5",
    "eslint-config-next": "^16.3.5",
    "eslint-config-prettier": "^10.1.8",
    "eslint-import-resolver-typescript": "^3.10.1",
    "eslint-plugin-boundaries": "^7.2.0",
    "jsdom": "^30.1.0",
    "pg": "^8.23.0",
    "prettier": "^3.9.8",
    "prisma": "7.10.0",
    "stylelint": "^17.15.0",
    "tsx": "^4.23.15",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.70.0",
    "vitest": "^5.0.1"
  },
  "allowScripts": {
    "unrs-resolver@1.12.2": true,
    "prisma@7.10.0": true,
    "@prisma/engines@7.10.0": true,
    "esbuild@0.28.2": true,
    "fsevents@2.3.3": false
  },
  "//": [
    "overrides: prisma@7.10.0 pins deepmerge-ts 7.1.5 (GHSA-ggr8-5vv4-36mx) and mysql2 3.15.3 (GHSA-3f6p-5ww8-9rcr, GHSA-rgwj-5xj2-c3m3); forcing the fixed releases keeps npm audit at 0 behind the T-02a gate (owner decision 2026-09-22).",
    "Re-measured 2026-09-24 (T-13): still needed — without them npm audit reports 4 high; prisma@7.10.0 (newest stable 7.x) pins deepmerge-ts 7.1.5 (via @prisma/config) and mysql2 3.15.3. Re-check: remove both overrides in a scratch copy, npm install --package-lock-only, npm audit --audit-level=high. Drop them when it is clean (owner: T-16).",
    "allowScripts: fsevents@2.3.3 (macOS only; its install is node-gyp rebuild) is denied — its tarball ships the prebuilt fsevents.node, which loads (T-13, measured 2026-09-24). .npmrc sets strict-allow-scripts=true, so a dependency with an install script that is not listed fails the install."
  ],
  "overrides": {
    "deepmerge-ts": "8.0.2",
    "mysql2": "3.24.4"
  }
}
